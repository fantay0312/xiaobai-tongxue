/**
 * 知识点:终端和 UNIX Shell(《操作系统原理》第 8 讲)—— 南京大学 jyy 2026 春。
 * 讲义:https://jyywiki.cn/OS/2026/lect8.md
 * 视频:https://www.bilibili.com/video/BV1nQXsBuEUz
 * 关键词组与本文件导出的 osShellDemo 预埋话术互相咬合,
 * 改动任一侧必须同步校验(scripts/simulate.ts)。
 *
 * 演示编排(与 attention 同构):
 *   ① c1 打字机血统 → ② c2 Ctrl-C 只是字符 + 金句(送信的邮差)→ 注入 M1(强杀开关,injectAfter c2)
 *   ③b 纠正 M1 + 顺势命中 c3(前台进程组)→ ④ 串讲无新命中(Lv5 在此轮提前迁移)
 *   ⑤ c4 Shell 翻译成系统调用 → 注入 M3(管道=临时文件,freshness 压过 M2)
 *   ⑥ 纠正 M3 + 命中 c5(壳在内核之外)→ 同轮衔接注入 M2(sudo 重定向)→ ⑦ 纠正 M2 收尾
 *
 * 内容红线(备课研究核定):
 *   - 不把「QWERTY 是为了故意放慢打字」当事实陈述(史学有争议,讲义口语梗),本讲干脆不讲 QWERTY;
 *   - 只说「回车/换行本来是两个动作」,不说「UNIX 的 \n 就是两个字符」——输出侧的合并
 *     是主流实现中终端驱动的翻译行为;
 *   - 「Ctrl-C 可以被程序接住」必须带分寸:SIGKILL/SIGSTOP 等个别强制信号不可捕获;
 *   - 「关窗口进程收到挂断信号」用「典型情况下」限定(会话/SIGHUP 语义有细碎例外);
 *   - 管道缓冲区 64KB 只说「Linux 上典型值」(出处 man 7 pipe);
 *   - 「shell 只是普通程序」按主流 UNIX / xv6 口径陈述,不绝对化到一切系统。
 */
import type { DemoLine, Topic } from '../../../types';
import type { SelfTestItem } from '../../selfTest';

export const osShellTopic: Topic = {
  topicId: 'os-shell',
  title: '终端和 UNIX Shell',
  course: '操作系统原理',
  tagline: 'Ctrl-C 的那一瞬间,动手的不是键盘',
  transferHint: '对智能音箱喊一句「关灯」,是谁听懂了话、又是谁去动的手',
  checklist: [
    {
      id: 'c1',
      point: '黑窗口的打字机血统',
      groundTruth:
        '终端源自电传打字机:键盘一头把按键变成字符送进计算机,另一头把字符打印/显示出来,' +
        '它只负责字符进出,本身不理解内容。回车(\\r)与换行(\\n)是打字机时代留下的两个动作:' +
        '回车把打印头移回行首,换行把纸向上卷一行;DEC 的 VT100(1978)确立了 80×24 字符布局' +
        '与转义序列的事实标准。',
      keywords: [
        ['打字机', '字符'],
        ['回车', '行首'],
        ['电传', '打印'],
      ],
      terms: ['终端', '打字机', '回车', '换行'],
      level: 'L1',
      lookupCard:
        '**黑窗口的打字机血统**\n\n今天的**终端**,祖上是电传**打字机**:键盘这头敲字,那头打印——' +
        '它只管**字符**进出,内容一概不懂。\n\n' +
        '- **回车**(\\r):把打印头拉回行首;\n' +
        '- **换行**(\\n):把纸往上卷一行——本来是两个动作,所以不同系统里"换行"的写法至今不一样;\n' +
        '- 1978 年的 VT100 把 80×24 字符的画面定成了行业标准,今天的终端窗口都是它的模仿者。',
      probeLine: '老师,我们每天对着的那个黑底白字的窗口,它到底是个什么来头呀?一生下来就长这样吗?',
    },
    {
      id: 'c2',
      point: 'Ctrl-C 只是一个字符',
      groundTruth:
        '终端不懂"结束程序":按下 Ctrl-C,送进系统的只是 3 号字符(ETX;Ctrl-D 是 4 号 EOT),' +
        '与字母没有本质区别。操作系统的终端驱动收到这个字符,才把它翻译成中断信号(SIGINT)发出去;' +
        '程序可以用 signal/sigaction 注册处理函数把信号接住——所以有的程序 Ctrl-C 退不出' +
        '(SIGKILL 等个别强制信号除外,它们设计上不可捕获)。',
      keywords: [
        ['Ctrl-C', '字符'],
        ['Ctrl+C', '字符'],
        ['3 号', '字符'],
        ['字符', '操作系统', '决定'],
      ],
      terms: ['Ctrl-C', 'ETX', '字符'],
      level: 'L2',
      lookupCard:
        '**Ctrl-C 只是一个字符**\n\n按下 **Ctrl-C**,送进系统的只是 3 号字符(**ETX**;Ctrl-D 是 4 号)' +
        '——终端和键盘都没有"杀程序"的本事。\n\n' +
        '真正动手的是操作系统:它收到这个**字符**,把它翻译成中断信号发出去;' +
        '程序可以提前登记信号处理函数把它接住——这就是有的程序 Ctrl-C 退不出的原因。\n\n' +
        '分寸:绝大多数信号接得住,但 SIGKILL 这类个别强制信号设计上不可捕获,底牌始终在系统手里。',
      probeLine: '那我按那个能让程序停下来的组合键时,到底是谁动的手呀?是键盘自己把程序掐掉的吗?',
    },
    {
      id: 'c3',
      point: '信号只打前台那一组',
      groundTruth:
        '操作系统给进程标了两层分组:会话(大分组,关联一个控制终端)和进程组(小分组);' +
        '一个终端同一时刻只有一个前台进程组。收到 Ctrl-C 时,内核向前台进程组的所有进程发送' +
        '中断信号(SIGINT),后台进程组不受影响;Ctrl-Z 发暂停信号(SIGTSTP),fg/bg 负责恢复与切换' +
        '——功能上很像窗口管理器的最小化与切换。',
      keywords: [
        ['前台', '进程组'],
        ['分组', '信号'],
        ['后台', '不受'],
      ],
      terms: ['信号', '进程组', '会话', 'SIGINT'],
      level: 'L3',
      lookupCard:
        '**信号只打前台那一组**\n\n操作系统给进程标了两层分组:\n\n' +
        '- **会话**(大分组):关联一个控制终端;\n' +
        '- **进程组**(小分组):一个终端同一时刻只有一个**前台进程组**。\n\n' +
        '收到 Ctrl-C,内核向前台进程组的**所有进程**发送 **SIGINT**,后台进程组不受影响;' +
        'Ctrl-Z 发暂停信号,fg/bg 负责恢复与切换——功能上就像窗口管理器的最小化和切换。',
      probeLine: '那要是我同时开着好几个程序,按下那个停止的组合键,电脑怎么知道该停哪一个、又不会误伤别的呀?',
    },
    {
      id: 'c4',
      point: 'Shell 把命令翻译成系统调用',
      groundTruth:
        'Shell 是一门基于文本替换的极简编程语言(只有字符串类型):它把你敲的命令翻译成系统调用——' +
        'fork 造子进程、execve 换上要运行的程序、waitpid 等它结束;重定向(cmd > file)是先打开文件、' +
        '再用 dup2 把文件描述符腾挪到标准输入/输出的位置;管道(cmd1 | cmd2)是内核里的一条缓冲通道,' +
        '两侧进程同时启动、并发运行,数据边产生边消费,不经过磁盘上的临时文件。',
      keywords: [
        ['翻译成', '系统调用'],
        ['fork', 'execve'],
        ['编程语言', '命令'],
      ],
      terms: ['Shell', 'shell', '系统调用', 'fork', 'execve', '管道', '重定向'],
      level: 'L3',
      lookupCard:
        '**Shell 把命令翻译成系统调用**\n\n**Shell** 是一门基于文本替换的极简编程语言' +
        '(只有字符串一种类型)。它干的活是翻译:\n\n' +
        '- **fork** 造子进程 → **execve** 换上要跑的程序 → waitpid 等收尾;\n' +
        '- **重定向**(cmd > file):先打开文件,再把子进程的出入口腾挪过去;\n' +
        '- **管道**(cmd1 | cmd2):内核里一条缓冲通道,两侧进程**同时启动、并发运行**,' +
        '数据边产生边消费,不落盘。',
      probeLine: '那我在黑窗口里敲的那一行行字,是谁听懂了它、又是谁把活儿派下去的呀?',
    },
    {
      id: 'c5',
      point: '壳在内核之外',
      groundTruth:
        'Shell 不是操作系统内核的一部分,它只是一个普通的用户程序:完全用系统调用' +
        '(fork/execve/pipe/dup2/waitpid)就能实现一个——xv6 就带一个零库函数依赖的 shell;' +
        '不喜欢还可以整个换掉(chsh),系统照常工作。这就是"壳"的含义:壳包在内核之外,' +
        '替人跟内核打交道;应用需求与操作系统 API 互相成就、螺旋生长。',
      keywords: [
        ['内核', '之外'],
        ['普通', '程序', '壳'],
        ['用户程序', '换'],
      ],
      terms: ['内核', 'xv6'],
      level: 'L5',
      lookupCard:
        '**壳在内核之外**\n\nShell 不是**内核**的一部分,它只是一个普通的用户程序:' +
        '完全用系统调用(fork/execve/pipe/dup2/waitpid)就能写出一个——教学系统 **xv6** 就带一个' +
        '零库函数依赖的 shell;不喜欢还能整个换掉(chsh),系统照常工作。\n\n' +
        '这就是"壳"的意思:壳包在内核外面,替人跟内核打交道。' +
        '应用的新需求催生系统的新本事——API 与应用互相成就、螺旋生长。',
      probeLine: '那这个听我们敲字、替我们跑腿的东西,是电脑最里头天生长着的一块吗?要是把它整个换掉,电脑还能转吗?',
    },
  ],
  misconceptions: [
    {
      mcId: 'os_shell_M1',
      topicId: 'os-shell',
      belief: 'Ctrl-C 是键盘自带的强杀开关,一按程序就当场断电,程序自己拦也拦不住',
      triggerLine:
        '哦!那我明白了:Ctrl-C 就是键盘上自带的强杀开关嘛,一按下去程序当场就断电了,' +
        '想拦也拦不住——跟直接拔插头是一个道理,对吧?',
      correctionCriteria: [
        '指出 Ctrl-C 送进系统的只是一个普通字符(3 号),终端和键盘都不会"动手"',
        '说明是操作系统把这个字符翻译成中断信号发出去的',
        '说明程序可以注册信号处理函数把信号接住,所以有的程序 Ctrl-C 退不出',
      ],
      correctionKeywords: [
        ['操作系统', '信号'], ['翻译成', '信号'], ['处理函数'], ['接住', '信号'],
      ],
      adoptionKeywords: [
        ['一按', '断电'], ['拦也拦不住'], ['强杀', '开关'],
      ],
      injectAfterChecklist: ['c2'],
      probe: {
        statement: 'Ctrl-C 是键盘自带的强制开关,一按下去程序必死,程序自己没有任何办法拦截。',
        isTrue: false,
        explanation:
          'Ctrl-C 送进系统的只是 3 号字符;操作系统把它翻译成中断信号发给前台进程组,' +
          '程序可以注册处理函数把它接住——真正拦不住的是 SIGKILL 这类个别强制信号。',
      },
      remedy: {
        microLesson: {
          title: '一个字符的旅行:从键盘到信号',
          body:
            '把 Ctrl-C 的旅程分成三站,"强杀开关"的错觉就散了:\n\n' +
            '```\n第一站 键盘/终端:只递字符 —— Ctrl-C = 3 号字符(跟字母 a 无本质区别)\n' +
            '第二站 操作系统:查按键绑定表(stty -a 可看),把字符翻译成中断信号\n' +
            '第三站 前台进程组:组里所有进程收到信号;默认动作才是退出\n```\n\n' +
            '关键在"默认"两个字:程序可以用 signal/sigaction **提前登记处理函数**,把信号接住——' +
            '接住之后爱干嘛干嘛,可以打印一行字,也可以做完清理再退。' +
            '这就是有的程序按 Ctrl-C 死活不退的原因:不是键盘失灵,是信号被接走了。\n\n' +
            '再补一个分寸:也不是所有信号都接得住——SIGKILL(kill -9)、SIGSTOP 这类强制信号' +
            '设计上不可捕获,系统永远给自己留了一张底牌。所以"拦不住"错在主体:' +
            '拦不拦得住,由信号的种类说了算,不由键盘说了算。',
          askBack: '下次小白再说「Ctrl-C 就是强杀开关」,你打算按哪三站把这趟旅行讲清楚?',
        },
        predictionQuiz: [
          {
            id: 'r1-1',
            question: '按下 Ctrl-C 的那一瞬间,终端(黑窗口)做了什么?',
            options: [
              '直接切断程序的电源',
              '把一个 3 号字符原样送进系统,别的什么都不做',
              '弹窗询问操作系统要不要杀',
              '先保存程序数据再退出',
            ],
            answerIndex: 1,
            explanation: '终端只管字符进出:Ctrl-C 在它眼里就是 3 号字符,决定权在操作系统手里。',
            checklistRef: 'c2',
            mcRef: 'os_shell_M1',
          },
          {
            id: 'r1-2',
            question: '有的程序按 Ctrl-C 死活退不出来,最可能的原因是?',
            options: [
              '键盘坏了',
              '它提前注册了信号处理函数,把中断信号接住了',
              '操作系统把它拉黑了',
              '这个程序的运行等级比操作系统还高',
            ],
            answerIndex: 1,
            explanation: '中断信号的"退出"只是默认动作;登记了处理函数就能接住它——强制信号(如 kill -9)才是底牌。',
            checklistRef: 'c2',
            mcRef: 'os_shell_M1',
          },
          {
            id: 'r1-3',
            question: '操作系统把这个中断信号发给谁?',
            options: [
              '电脑上所有正在跑的程序',
              '随机挑一个倒霉蛋',
              '当前终端的前台进程组里的所有进程,后台不受影响',
              '只发给最占内存的那一个',
            ],
            answerIndex: 2,
            explanation: '会话套进程组的两层分组,就是为了让信号只打前台那一组、不误伤后台。',
            checklistRef: 'c3',
            mcRef: null,
          },
        ],
      },
    },
    {
      mcId: 'os_shell_M2',
      topicId: 'os-shell',
      belief: '命令前面加上 sudo,整条命令连同重定向都是管理员身份,往系统文件里写肯定成',
      triggerLine:
        '可是老师,有个事我越想越顺:要往系统那边的文件里写东西,前面加个 sudo 不就齐活了嘛?' +
        '比如 sudo echo hello,再用那个往文件里倒东西的小箭头指到系统文件里——sudo 一出手,' +
        '整条命令从头到尾都是管理员在跑,小箭头肯定也跟着沾光,写进去妥妥的,对吧?',
      correctionCriteria: [
        '指出重定向是 shell 干的,发生在命令启动之前',
        '说明 shell 以当前普通用户的身份打开目标文件,打不开整条命令就失败,sudo 只提升了后面那个命令',
        '(可选)给出正确姿势:echo hello | sudo tee 或 sudo sh -c',
      ],
      correctionKeywords: [
        ['shell', '干的'], ['Shell', '干的'], ['启动', '之前', '打开'], ['普通用户', '身份'],
      ],
      adoptionKeywords: [
        ['都是管理员'], ['跟着沾光'], ['肯定写得进'],
      ],
      injectAfterChecklist: ['c1', 'c2', 'c3', 'c4'],
      probe: {
        statement: '只要在整条命令前加 sudo,重定向到系统文件就一定能成功,因为重定向也是以管理员身份执行的。',
        isTrue: false,
        explanation:
          '重定向由 shell 在启动命令之前完成,shell 还是普通用户身份,打开系统文件那一步就失败了;' +
          'sudo 只提升了后面的命令——想写进去要用 echo hello | sudo tee 或 sudo sh -c。',
      },
      remedy: {
        microLesson: {
          title: '先搭水管,再开水龙头:重定向是 shell 干的',
          body:
            '一条带重定向的命令,诞生顺序是固定的:\n\n' +
            '```\n$ sudo echo hello > /etc/a.txt\n' +
            '第 1 步 shell 解析这行字,看见 >\n' +
            '第 2 步 shell 以【你的普通用户身份】打开 /etc/a.txt ← 在这里就失败了\n' +
            '第 3 步 (没有第 3 步:echo 和 sudo 压根没上场)\n```\n\n' +
            '**水管是 shell 先接好的,水龙头(命令)后拧开**——sudo 只罩得住后面那个命令,' +
            '罩不住正在接水管的 shell 本人。所以报错的不是 echo,是 shell:Permission denied。\n\n' +
            '想真的写进去,得让"提了权的人"去开文件:\n\n' +
            '```\n$ echo hello | sudo tee /etc/a.txt        # 让管理员身份的 tee 来写\n' +
            '$ sudo sh -c \'echo hello > /etc/a.txt\'    # 整段活儿交给管理员身份的 shell\n```\n\n' +
            '这个坑的价值在于它逼你想清楚:每一个符号到底是谁在处理、以谁的身份处理。',
          askBack: '下次小白再说「加个 sudo 就万事大吉」,你打算怎么用"先搭水管再开水龙头"讲清顺序?',
        },
        predictionQuiz: [
          {
            id: 'r2-1',
            question: 'sudo echo hello > /etc/a.txt 为什么照样被拒?',
            options: [
              'echo 不支持 sudo',
              '重定向由 shell 以普通用户身份先执行,打开文件那一步就失败了',
              '/etc 目录根本不存在',
              'hello 这个词拼写错误',
            ],
            answerIndex: 1,
            explanation: '水管是 shell 先接的:它还是普通用户,开不了 /etc/a.txt,echo 压根没上场。',
            checklistRef: 'c4',
            mcRef: 'os_shell_M2',
          },
          {
            id: 'r2-2',
            question: '想把一行字写进需要管理员权限的文件,下面哪种写法真的可行?',
            options: [
              'sudo echo hello > 目标文件',
              'echo hello | sudo tee 目标文件',
              '把 sudo 连写两遍加强语气',
              '先按 Ctrl-C 再重试一次',
            ],
            answerIndex: 1,
            explanation: '让提了权的 tee 去写文件(或 sudo sh -c 把整段交给管理员身份的 shell)才能绕开这个坑。',
            checklistRef: 'c4',
            mcRef: 'os_shell_M2',
          },
          {
            id: 'r2-3',
            question: '关于 shell 和内核的关系,哪个说法是对的?',
            options: [
              'shell 是内核里拆不掉的一部分',
              'shell 只是个普通用户程序,用系统调用就能自己写一个,还能整个换掉',
              'shell 坏了电脑就得重装系统',
              'shell 的权限比内核还高',
            ],
            answerIndex: 1,
            explanation: '壳在内核之外:xv6 就带一个零依赖 shell,换一个 shell 系统照常工作。',
            checklistRef: 'c5',
            mcRef: null,
          },
        ],
      },
    },
    {
      mcId: 'os_shell_M3',
      topicId: 'os-shell',
      belief: '管道就是先把左边的命令跑完,把结果存进一个临时文件,右边的命令再去读这个文件',
      triggerLine:
        '那这个我懂了:中间那根竖线嘛,说白了就是先把左边那个命令跑完,把结果存到一个临时文件里,' +
        '右边那个再去读这个文件——跟我平时先导出再导入是一个路数,对吧?',
      correctionCriteria: [
        '指出管道中间没有临时文件,数据不经过磁盘',
        '说明两侧进程是同时启动、并发运行的,数据边产生边消费',
        '(可选)缓冲通道满了写端会被暂停,等读端消化(背压)',
      ],
      correctionKeywords: [
        ['没有', '临时文件'], ['同时', '启动'], ['边产', '边'], ['并发', '通道'],
      ],
      adoptionKeywords: [
        ['先跑完', '临时文件'], ['存到', '文件', '再读'], ['导出', '导入'],
      ],
      injectAfterChecklist: ['c4'],
      probe: {
        statement: '竖线管道的工作方式,是先把左边命令的全部输出写进一个临时文件,再让右边命令去读它。',
        isTrue: false,
        explanation:
          '管道是内核里的一条缓冲通道:两侧进程同时启动、并发运行,数据边写边读、不落盘;' +
          '缓冲满了写端还会被暂停——所以 cat 超大文件 | head -1 几乎瞬间返回。',
      },
      remedy: {
        microLesson: {
          title: '管道不是临时文件,是流水线',
          body:
            '把两种想象摆在一起,一眼就能分出真假:\n\n' +
            '```\n【误解】cmd1 全部跑完 → 写满临时文件 → cmd2 再开始   (串行,落盘)\n' +
            '【实际】cmd1 ══ 内核缓冲通道 ══ cmd2                  (并发,不落盘)\n```\n\n' +
            '竖线两边的程序是**同时启动**的:左边一边产出,右边一边消化,' +
            '数据从内核里的一条缓冲通道直接流过去(Linux 上容量典型 64KB);' +
            '右边吃不动了,左边会被暂停下来等它——这叫背压,流水线自己会调速。\n\n' +
            '当场可验:`cat 超大日志.log | head -1` 一眨眼就返回。' +
            '要是"先存临时文件"成立,就得先读完整个大文件再说;实际上 head 拿到第一行就退出,' +
            'cat 也跟着被叫停,一秒都不多干。\n\n' +
            '这正是 UNIX 哲学的落地:每个程序只做一件事,做好它;拿文本流当万能接口,' +
            '把小工具像积木一样串起来。',
          askBack: '下次小白再说「竖线就是先导出再导入」,你打算用哪条命令当场演示给它看?',
        },
        predictionQuiz: [
          {
            id: 'r3-1',
            question: '跑 cat 超大文件 | head -1,几乎一眨眼就返回了,为什么?',
            options: [
              '电脑性能好,读完几个 GB 只要一瞬',
              '两边同时开工:head 拿到第一行就收工,cat 也跟着被叫停',
              'head 会跳过前面的内容不读',
              '结果是上一次的缓存,根本没跑',
            ],
            answerIndex: 1,
            explanation: '管道两侧并发运行,数据边产边流;读端退出后写端也会被叫停,不做无用功。',
            checklistRef: 'c4',
            mcRef: 'os_shell_M3',
          },
          {
            id: 'r3-2',
            question: '管道里流动的数据,会先写到磁盘上的临时文件里吗?',
            options: [
              '会,不然右边读什么',
              '不会——数据走内核里的缓冲通道,不落盘',
              '取决于文件大小',
              '只有硬盘空闲的时候才会',
            ],
            answerIndex: 1,
            explanation: '管道是内核里的缓冲通道(Linux 上典型 64KB),数据从头到尾不经过磁盘。',
            checklistRef: 'c4',
            mcRef: 'os_shell_M3',
          },
          {
            id: 'r3-3',
            question: '左边产得太快、右边消化不动时,会发生什么?',
            options: [
              '多出来的数据直接丢掉',
              '缓冲通道满了,左边被暂停,等右边消化(背压)',
              '操作系统当场死机',
              '右边会被强制加速',
            ],
            answerIndex: 1,
            explanation: '缓冲有限,写端会被暂停——流水线靠背压自动调速,谁也不丢数据。',
            checklistRef: 'c4',
            mcRef: null,
          },
        ],
      },
    },
  ],
  quizBank: [
    {
      id: 'q1',
      question: '终端的祖上是电传打字机,这个血统决定了它骨子里只做什么?',
      options: [
        '理解命令的含义并亲自执行',
        '把字符送进送出,内容一概不懂',
        '管理所有程序的生死',
        '存储你电脑上的所有文件',
      ],
      answerIndex: 1,
      explanation: '终端只管字符进出:敲键变字符送进去,吐出来的字符显示出来——动脑子的另有其人。',
      checklistRef: 'c1',
      mcRef: null,
    },
    {
      id: 'q2',
      question: '「Ctrl-C 结束了程序」——这句话里,真正动手的是谁?',
      options: [
        '键盘:一按就物理断电',
        '终端:它认识 Ctrl-C 这个手势',
        '操作系统:它收到 3 号字符,把它翻译成中断信号发出去',
        '程序自己:吓得不敢跑了',
      ],
      answerIndex: 2,
      explanation: '键盘和终端都只递字符;把字符变成信号、决定发给谁的,是操作系统。',
      checklistRef: 'c2',
      mcRef: 'os_shell_M1',
    },
    {
      id: 'q3',
      question: '一个终端里既有正在前面跑的程序,也有挂在后面的任务,Ctrl-C 打到谁身上?',
      options: [
        '打到前台进程组的所有进程身上,后台那组不受影响',
        '打到这台电脑的所有进程身上',
        '随机打一个',
        '谁最吵打谁',
      ],
      answerIndex: 0,
      explanation: '会话套进程组的两层分组,让信号只发给前台那一组——这正是后台任务安然无恙的原因。',
      checklistRef: 'c3',
      mcRef: null,
    },
    {
      id: 'q4',
      question: '用竖线把两条命令接起来(cmd1 | cmd2),数据是怎么流的?',
      options: [
        '先把左边跑完,存进临时文件,右边再读',
        '两边同时启动,数据经内核缓冲通道边产边流,不落盘',
        '左边把数据打印到屏幕,右边拿相机拍下来',
        '右边先跑,左边再补作业',
      ],
      answerIndex: 1,
      explanation: '管道是内核里的缓冲通道,两侧并发运行——cat 超大文件 | head -1 秒回就是铁证。',
      checklistRef: 'c4',
      mcRef: 'os_shell_M3',
    },
    {
      id: 'q5',
      question: '为什么 sudo echo hello > /etc/a.txt 还是提示没权限?',
      options: [
        'sudo 的拼写有讲究',
        '重定向是 shell 在启动命令之前、以普通用户身份做的,打开文件那一步就失败了',
        '/etc/a.txt 被加密了',
        'echo 没有输出功能',
      ],
      answerIndex: 1,
      explanation: '水管是 shell 先接的:sudo 只提升了 echo,罩不住 shell 本人——要用 sudo tee 或 sudo sh -c。',
      checklistRef: 'c4',
      mcRef: 'os_shell_M2',
    },
    {
      id: 'q6',
      question: '下面对 Shell 的定位,哪个说得最准?',
      options: [
        '它是内核里最核心的模块',
        '它是壳:内核之外的一个普通用户程序,把命令翻译成系统调用,还能整个换掉',
        '它是一种电脑硬件',
        '它是杀毒软件的别名',
      ],
      answerIndex: 1,
      explanation: '壳在内核之外:用 fork/execve 这些系统调用就能自己写一个,xv6 的零依赖 shell 就是活例。',
      checklistRef: 'c5',
      mcRef: null,
    },
  ],
  prep: {
    microLecture: {
      title: '五分钟看懂:一台打字机,怎么长成了操作系统的门面',
      body:
        '把这一讲想成"一个字符的权力之旅",五个要点就串起来了:\n\n' +
        '1. **终端是打字机的后代**。黑窗口的祖上是电传打字机:键盘这头敲字,那头打印,' +
        '它只管字符进出、内容一概不懂。回车(把打印头拉回行首)和换行(把纸往上卷一行)' +
        '本来是两个动作;1978 年的 VT100 把 80×24 字符的布局定成了行业标准。\n' +
        '2. **Ctrl-C 只是个字符**。按下 Ctrl-C,送进系统的只是 3 号字符(Ctrl-D 是 4 号);' +
        '终端和键盘都没有"杀程序"的本事。是操作系统把这个字符翻译成中断信号发出去;' +
        '程序可以注册信号处理函数把它接住——所以有的程序 Ctrl-C 退不出' +
        '(SIGKILL 等个别强制信号除外)。\n' +
        '3. **信号只打前台那一组**。操作系统给进程标了大分组(会话,关联控制终端)和小分组(进程组);' +
        '一个终端同一时刻只有一个前台进程组。Ctrl-C 发给前台进程组的所有进程,后台不受影响;' +
        'Ctrl-Z 暂停、fg/bg 切换,像窗口管理器的最小化与切换。\n' +
        '4. **Shell 把命令翻译成系统调用**。Shell 是一门基于文本替换的极简编程语言(只有字符串类型):' +
        'fork 造子进程、execve 换上程序、waitpid 等收尾;重定向是先开文件再把出入口腾挪过去;' +
        '管道是内核里的缓冲通道,两侧并发,边产边吃,不落盘。\n' +
        '5. **壳在内核之外**。Shell 只是个普通用户程序,完全用系统调用就能写一个' +
        '(xv6 就带一个零依赖 shell);不喜欢能整个换掉,系统照常跑。应用需求与系统 API 互相成就。\n\n' +
        '判断口诀:**终端管字符,系统管信号,前台一组挨打,Shell 只做翻译,壳在内核之外**。' +
        '小白最容易问倒你的地方:「Ctrl-C 是不是键盘自带的强杀开关?」\n\n' +
        '**讲课节奏建议**\n\n' +
        '- **先讲①②,把"传话链"的画面立起来**:①用打字机血统说清"终端只管字符进出",' +
        '②紧跟着抛出 Ctrl-C——它只是 3 号字符,真正拿主意的是操作系统。这两步是全篇地基。\n' +
        '- **②讲完,警报拉响**:小白八成会顺着日常经验起哄——「一按就断电,拦也拦不住!」' +
        '别被"强杀开关"的直觉带跑:先说清字符→信号的翻译,再补一刀"程序能登记处理函数把信号接住",' +
        '顺势把③"只发给前台那一组"讲透。\n' +
        '- **中场把"键盘→字符→操作系统→信号"串一遍**再进④:链路复述对了再往前走。\n' +
        '- **讲完④,它还会来两次**:先是「管道就是先存临时文件吧?」用 cat 超大文件 | head -1 ' +
        '秒回的例子顶回去;紧接着多半还嘀咕「加个 sudo 就万事大吉」——用"重定向是 shell ' +
        '以普通用户身份先做的"收尾,别忘了给出 sudo tee 的正确姿势。\n\n' +
        '**一句话收束**\n\n' +
        '**终端只管把字符送进送出,操作系统负责把字符翻成信号、找准前台那一组;' +
        'Shell 把你敲的命令翻译成系统调用——而它自己,只是内核之外的一层壳。**\n\n' +
        '**再深一锹(选读)**\n\n' +
        '- **伪终端:想要多少有多少**。图形界面里每开一个终端窗口,系统就造一对"管道"——' +
        '一头接窗口程序,一头接 shell;把流出的字符连时间戳录下来,就是一个现成的"录屏器"。\n' +
        '- **行编辑是系统"模拟"出来的**。你在命令行里退格改字,程序并没有看见每一次按键——' +
        '主流实现里是系统的终端驱动攒好一整行才交货;关掉这层加工就是"按一个键立刻送一个"' +
        '的原始模式,全屏编辑器就是这么活的。\n' +
        '- **历史的糟粕也是标准的一部分**。会话/进程组这套设计诞生时还没有伪终端,如今看来绕得很;' +
        '但它进了 POSIX,谁也甩不掉——几乎没有人能预知软件的未来。\n' +
        '- **文本的代价**。Shell 拿字符串当万能接口,好处是万物可拼,代价是"空格 = 灾难":' +
        '文件名带个空格,脚本就可能拆错词;老前辈的取舍,后人将错就错。',
    },
    examples: [
      {
        title: '例 1:接住 Ctrl-C 的程序(信号处理)',
        code:
          '#include <signal.h>\n#include <stdio.h>\n\n' +
          'void on_int(int sig) {\n' +
          '  printf("想得美,我接住了\\n");   // 不退出,继续跑\n' +
          '}\n\n' +
          'int main() {\n' +
          '  signal(SIGINT, on_int);          // 登记:中断信号归我处理\n' +
          '  while (1) { /* 死循环 */ }\n' +
          '}\n' +
          '// 按 Ctrl-C:只打印一行字,程序照跑不误',
        walkthrough:
          '按键旅程:键盘 → 终端(递进 3 号字符)→ 操作系统(翻译成 SIGINT)→ 前台进程组。' +
          '程序提前登记了处理函数,信号被接住,自然不退。教学分寸:别把话说死——' +
          'SIGKILL(kill -9)这类强制信号设计上不可捕获,系统永远留着底牌。',
      },
      {
        title: '例 2:重定向的腾挪(shell 在命令上场前动手)',
        code:
          'int fd = open("a.txt", O_WRONLY);   // shell 先把文件打开\n' +
          'int pid = fork();                    // 造一个子进程\n' +
          'if (pid == 0) {\n' +
          '  dup2(fd, 1);                       // 把 1 号出口(标准输出)换接到文件\n' +
          '  execve("/bin/ls", ...);            // 换上 ls:它以为自己在往屏幕打印\n' +
          '} else {\n' +
          '  close(fd);\n' +
          '  waitpid(pid, &status, 0);          // 父进程等孩子跑完\n' +
          '}',
        walkthrough:
          'ls 从头到尾不知道自己被重定向了:它照常往 1 号出口写,出口通向哪里是 shell 提前布好的。' +
          '这正是 sudo echo hello > /etc/a.txt 失败的根源——开文件的是 shell(普通用户),' +
          'sudo 只提升了 echo;想写进去,用 echo hello | sudo tee /etc/a.txt,' +
          '或 sudo sh -c 让提了权的 shell 去开文件。',
      },
      {
        title: '例 3:管道是流水线,不是临时文件',
        code:
          '$ cat 超大日志.log | head -1      # 一眨眼就返回\n\n' +
          '# 若管道 = 先存临时文件:得先读完整个文件、再写一遍,才轮到 head\n' +
          '# 实际:cat 和 head 同时启动;head 拿到第一行就退出,cat 跟着被叫停',
        walkthrough:
          '管道是内核里的一条缓冲通道(Linux 上容量典型 64KB):左边边产、右边边吃,' +
          '数据不经过磁盘;右边吃不动,左边会被暂停(背压)。这也是 UNIX 哲学的落地:' +
          '每个程序只做一件事,用文本流把小工具串成流水线。',
      },
    ],
    selfCheck: [
      '回车和换行本来各是什么动作?能讲出它们的打字机来历吗?',
      '「Ctrl-C 杀掉了程序」这句话错在哪?字符→信号→前台进程组这条链,你能不打磕巴地走完吗?',
      '小白要是坚持「管道就是先存临时文件」,你准备好 cat 超大文件 | head -1 这个当场演示了吗?',
      'sudo echo hello > /etc/a.txt 为什么失败?正确姿势(sudo tee / sudo sh -c)能随手写出来吗?',
      '"壳在内核之外"怎么讲才有画面感?fork、execve、waitpid 三步各干什么,你能按顺序说清吗?',
    ],
    taskCard:
      '📋 你的教学任务:等会小白会问你——「Ctrl-C 是不是键盘自带的强杀开关,一按程序就当场断电,' +
      '想拦也拦不住?」带着这个问题去读下面的材料,想好你打算怎么给它讲明白。' +
      '纠不动它,它会开心地把错的学走。',
    references: [
      {
        title: '终端和 UNIX Shell',
        url: 'https://jyywiki.cn/OS/2026/lect8.md',
        kind: '讲义',
        note: '本讲原版讲义:从打字机血统一路讲到 Ctrl-C、会话/进程组与零依赖 shell,课堂梗与 Demo 都在这里。',
      },
      {
        title: '08 (Aside) - 终端和 UNIX Shell [2026 南京大学操作系统原理]',
        url: 'https://www.bilibili.com/video/BV1nQXsBuEUz',
        kind: '视频',
        note: 'UP 主「绿导师原谅你了」(jyy 官方号):本讲课堂实录,"Ctrl-C 杀不掉的程序"与 stty 演示值得亲眼看一遍。',
      },
      {
        title: 'The TTY demystified',
        url: 'https://www.linusakesson.net/programming/tty/',
        kind: '长文',
        note: '把终端、行编辑、会话与作业控制的来龙去脉讲成侦探故事的经典长文——读完 c2/c3 这条链就全通了。',
      },
      {
        title: 'Operating Systems: Three Easy Pieces',
        url: 'https://pages.cs.wisc.edu/~remzi/OSTEP/',
        kind: '教程',
        note: '免费在线的《操作系统导论》官网:重点读 Interlude: Process API 一章(fork/exec/wait 与"为什么 shell 能轻松做重定向"),与本讲 Shell 翻译部分一一对应。',
      },
      {
        title: 'signal(7) - Linux manual page',
        url: 'https://man7.org/linux/man-pages/man7/signal.7.html',
        kind: '官方文档',
        note: 'Linux 信号的官方清单:哪些信号可捕获、SIGKILL/SIGSTOP 为何拦不住——讲 Ctrl-C 之前查一眼,分寸就有了。',
      },
      {
        title: '课程概览与 shell',
        url: 'https://missing-semester-cn.github.io/2020/course-shell/',
        kind: '教程',
        note: 'MIT《计算机教育中缺失的一课》中文版第一讲:上手侧的 shell 入门,重定向、管道、sudo tee 的坑都有,适合课后动手练。',
      },
    ],
  },
};

export const osShellDemo: DemoLine[] = [
  {
    label: '① 讲:黑窗口的打字机血统',
    text:
      '我们先从这个黑窗口的来头说起。它祖上是电传打字机——键盘这头敲字,那头把字符一个个打印出来。' +
      '所以它骨子里只干两件事:把你按的键变成字符送进计算机,把计算机吐出来的字符显示出来,' +
      '内容它一概不懂。连回车和换行都是打字机时代留下来的:回车是把打印头拉回行首,' +
      '换行是把纸往上卷一行,本来是两个不同的动作。',
    note: '命中 c1(打字机血统/回车换行)→ 小白开窍复述,追问 c2(谁动的手让程序停下)',
  },
  {
    label: '② 讲:Ctrl-C 只是个字符',
    text:
      '接着说一个特别有意思的事:你按 Ctrl-C 的时候,这个窗口其实什么都没有"杀"。' +
      '它就像一个只管送信的邮差,只负责把字符原样递进去——Ctrl-C 在它眼里就是编号为 3 的一个字符,' +
      '跟字母 a 没有本质区别。真正看到这个字符、决定要不要出手的,是站在后面的操作系统。',
    note: '命中 c2 + 金句类比收录(只管送信的邮差)→ 触发 M1 注入:「键盘自带的强杀开关?」(高潮)',
  },
  {
    label: '③a 被带偏(演示盲区暴露)',
    text:
      '呃……好像还真是,平时程序卡死了,我一按 Ctrl-C 它立马就没了……那我记住了:' +
      '这就是一按就断电的强杀开关,程序想拦也拦不住,多干脆。',
    note: 'M1 判定「被带偏」(命中「一按+断电/拦也拦不住」)→ 小白开心地学错了,盲区落档(severity=high),考小白关联题必错',
  },
  {
    label: '③b 正确纠正(对照分支)',
    text:
      '不对不对,键盘可没有那么大的权力。那个 3 号字符被送到操作系统手里,操作系统才把它翻译成一个' +
      '"中断信号"发出去;程序可以提前登记一个信号处理函数,把这个信号接住——所以有的程序你按 Ctrl-C ' +
      '根本退不出来。而且这个信号也不是见人就发:操作系统把进程编成了组,一个窗口同一时刻只有一个' +
      '前台进程组,信号只发给前台进程组里的所有进程,后台那组完全不受影响。',
    note: 'M1 判定「已纠正」(命中「操作系统+信号/处理函数」)+ 顺势命中 c3(前台进程组)→ Aha 复述后追问 c4,纠错力 +1',
  },
  {
    label: '④ 串讲:一个字符的完整旅程',
    text:
      '我把前面的串一串:首先,这个黑窗口祖上是打字机,只管把字符送进送出,自己不动脑子;' +
      '然后,你按下 Ctrl-C,它也只是照章递进去一个 3 号字符;最后,真正拿主意的是操作系统——' +
      '它看到这个字符,就给前台那一组进程发信号,后台的照常干活。所以整条链路是:' +
      '键盘、字符、操作系统、信号,一环扣一环,每一环都只做自己的分内事。',
    note: '复述巩固(无新命中,词汇与主题有交集不致误判偏题)→ 小白就 c4(那行字谁听懂的)发起边界追问;Lv5 人格在此轮提前发起迁移追问',
  },
  {
    label: '⑤ 讲:Shell 把命令翻译成系统调用',
    text:
      '接下来讲讲真正听懂你敲字的那位:Shell。它其实是一门编程语言,只有字符串一种类型,' +
      '干的活就是把你敲的每一条命令翻译成系统调用:用 fork 造出一个子进程,用 execve 给子进程' +
      '换上要运行的程序,再用 waitpid 等它跑完;像 ls > a.txt 这样的重定向,就是先把文件打开,' +
      '再把子进程的输出口腾挪到这个文件上;两条命令之间还可以用竖线接一根管道,让数据从左边流到右边。',
    note: '命中 c4(翻译成系统调用/fork+execve)→ 触发 M3 注入:「管道就是先存临时文件?」',
  },
  {
    label: '⑥ 纠正 M3 + 讲:壳在内核之外',
    text:
      '不对,管道中间根本没有临时文件,数据也不经过磁盘。竖线两边的程序是同时启动、一起开工的:' +
      '左边一边产出,右边一边消化,数据从内核里的一条缓冲通道直接流过去;右边吃不动了,' +
      '左边还会被暂停下来等它。也就是说,这是一条活的流水线,不是先装满一桶再倒给下家。' +
      '另外还有件挺妙的事:Shell 本身并不是操作系统最核心的那部分,它就是一个普通的用户程序,' +
      '完全用刚才那几个系统调用就能自己写出一个来,不喜欢还能整个换掉,系统照常转——' +
      '所以它才叫"壳":壳在内核之外,包着内核替我们跟它打交道。',
    note: '纠正 M3(命中「没有+临时文件/同时+启动」)+ 命中 c5(壳在内核之外)→ 同轮衔接注入 M2(加个 sudo 就万事大吉?)',
  },
  {
    label: '⑦ 纠正 M2(收尾)',
    text:
      '还真不成,sudo echo hello 加上那个小箭头照样会被拒之门外。那个往文件里倒东西的小箭头,' +
      '不是 echo 干的,是 shell 干的:shell 在把命令启动起来之前,就先以你这个普通用户的身份' +
      '去打开 /etc/a.txt,把水管提前接好;打不开,这一步就直接失败了,echo 压根没上场。' +
      'sudo 只提升了 echo 一个人的身份,shell 自己还是普通用户。真想写,可以让提了权的人自己去开文件,' +
      '比如用 sudo tee,或者干脆 sudo sh -c 把整段活儿都交给一个管理员身份的 shell。',
    note: '纠正 M2(命中「shell+干的/普通用户+身份」)→ 三误区全纠正、五要点全命中,可下课考小白(预期 100 分出师)',
  },
  {
    label: '卡壳演示(触发 R1 救援)',
    text: '嗯……这一段我卡住了,一下子想不起来后面该怎么讲了……',
    note: '卡壳信号 → 小白递台阶(R1);连续两次 → 一起查书(R2)',
  },
  {
    label: '偏题演示(内容围栏)',
    text: '小白,操场边的桂花开了,晚饭后要不要一起去湖边走走,顺路再买两串糖葫芦?',
    note: '偏题 → 小白角色内拉回:「这跟今天的知识点没关系吧」',
  },
];

/**
 * 摸底快测题组(聚合于 src/data/selfTest.ts)。
 * 出题纪律:不与 quizBank / probe 题干重复,只考同一要点的另一侧面;
 * 干扰项须是"听着有道理的常见误解",每题只留一个略带幽默的。
 * 分寸沿用文件头内容红线:可捕获要提强制信号例外,SIGHUP 用「典型情况下」限定。
 */
export const osShellSelfTest: SelfTestItem[] = [
  {
    id: 'st1',
    dimension: '概念',
    question: '打字机时代留下的「回车」和「换行」,各自本来是什么动作?',
    options: [
      '回车=把打印头拉回行首,换行=把纸往上卷一行——本来是两个动作',
      '都是把纸吐出来的意思',
      '回车=删掉一行,换行=加一行',
      '回车是英文的,换行是中文的',
    ],
    answerIndex: 0,
    explanation: '一个横着回位,一个竖着走纸,各管一半——这也是不同系统里"换行"写法至今不一样的历史根子。',
    checklistRef: 'c1',
    mcRef: null,
  },
  {
    id: 'st2',
    dimension: '推演',
    question: '只按住 Shift 键不放,有东西被送进电脑吗?',
    options: [
      '有,送进一个"Shift 字符"',
      '没有——得配上字母才真正送出一个字符,这是打字机抬字模的遗产',
      '有,送进一串省略号',
      '看键盘贵不贵',
    ],
    answerIndex: 1,
    explanation: '打字机上 Shift 只是把字模抬起来切换字符集;单按 Shift,没有任何数据进电脑。',
    checklistRef: 'c1',
    mcRef: null,
  },
  {
    id: 'st3',
    dimension: '边界',
    question: '一个程序把中断信号接住了,按多少次 Ctrl-C 都不退——它就真的无敌了吗?',
    options: [
      '是,只能重启电脑',
      '不是——还有个别设计成接不住的强制信号兜底,比如那记著名的 9 号',
      '是,除非拔电源',
      '不是,多按几次就能累积伤害',
    ],
    answerIndex: 1,
    explanation: '绝大多数信号可以被接住,但 SIGKILL 这类强制信号设计上不可捕获——系统给自己留了底牌。',
    checklistRef: 'c2',
    mcRef: 'os_shell_M1',
  },
  {
    id: 'st4',
    dimension: '应用',
    question: '你按 Ctrl-C 停掉了正在前面刷屏的程序,挂在后面下载的任务却一点没受影响——靠的是哪种安排?',
    options: [
      '运气好,信号没瞄准',
      '操作系统给进程分了组,信号只发给前台那一组',
      '下载程序反应快,躲开了',
      '后台程序穿了防弹衣',
    ],
    answerIndex: 1,
    explanation: '会话套进程组的两层分组,让 Ctrl-C 只打前台进程组的所有进程,后台组毫发无损。',
    checklistRef: 'c3',
    mcRef: null,
  },
  {
    id: 'st5',
    dimension: '辨析',
    question: '同事说:「竖线就是先把左边全部跑完、存成文件,再喂给右边。」当场最有说服力的反驳实验是?',
    options: [
      '跟他拍桌子,嗓门大者胜',
      '跑一条 cat 超大文件 | head -1:一眨眼返回,说明两边是同时开工的流水线',
      '把电脑重启一遍给他看',
      '让他把这句话再大声说三遍',
    ],
    answerIndex: 1,
    explanation: '若真要先存临时文件,就得先读完整个大文件;秒回说明两侧并发、边产边流——事实胜于雄辩。',
    checklistRef: 'c4',
    mcRef: 'os_shell_M3',
  },
  {
    id: 'st6',
    dimension: '应用',
    question: '想亲手写一个能跑命令的迷你壳,最少要向操作系统借哪几样本事?',
    options: [
      '造子进程、给子进程换上程序、等子进程收尾这几个系统调用就够了',
      '必须先修改内核源码',
      '要有一块高端显卡',
      '得先考取键盘制造许可证',
    ],
    answerIndex: 0,
    explanation: 'fork/execve/waitpid(加上管道重定向用的 pipe/dup2)就够了——xv6 的零依赖 shell 就是活例。',
    checklistRef: 'c5',
    mcRef: null,
  },
  {
    id: 'st7',
    dimension: '边界',
    question: '直接把整个黑窗口一关,里面还在跑的程序一般会怎样?',
    options: [
      '什么都不发生,继续默默跑',
      '典型情况下会收到一记「挂断」信号,跟着一起谢幕',
      '程序会自动存档再优雅退出',
      '变成幽灵进程,谁也抓不到它',
    ],
    answerIndex: 1,
    explanation: '窗口没了,关联的会话也散伙了——典型情况下相关进程会收到挂断信号;想留下来得提前"托孤"(如 nohup)。',
    checklistRef: 'c3',
    mcRef: null,
  },
];
