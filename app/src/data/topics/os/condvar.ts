/**
 * 知识点:并发控制——条件变量(《操作系统原理》第 15 讲)—— 万能同步方法。
 * 讲义:https://jyywiki.cn/OS/2026/lect15.md
 * 视频:https://www.bilibili.com/video/BV1t3dQBAEzd
 * 关键词组与本文件导出的 osCondvarDemo 预埋话术互相咬合,
 * 改动任一侧必须同步校验(scripts/simulate.ts)。
 *
 * 演示编排(与 attention / os-process 同构):
 *   ① c1(同步管先后 vs 互斥管独占)→ ② c2(别自旋,cond_wait 躺下等叫醒)+金句
 *     → 注入 M1(cond_wait = 先解锁再自己睡两步?injectAfter c2)
 *   ③b 纠正 M1(原子性/丢失唤醒)+ 顺势命中 c3(万能模板)→ ④ 串讲无新命中(Lv5 在此轮提前迁移)
 *   ⑤ c4(为什么 while 不用 if)→ 注入 M3(被唤醒就说明条件成立?injectAfter c4)
 *   ⑥ 纠正 M3 + 命中 c5(生产者-消费者)→ 同轮衔接注入 M2(只 signal 一个够了?)→ ⑦ 纠正 M2 收尾
 *
 * 内容红线(备课研究核定,以 jyy 2026 讲义 + OSTEP 第 30 章为准):
 *   - cond_wait「原子地释放锁并进入等待」是语义要点;拆成 unlock()+sleep() 会有丢失唤醒(lost wakeup),
 *     不说「一定死锁」,只说「可能永远睡下去 / 可能丢唤醒」;
 *   - 等待一律用 while 重新检查,不用 if:一因别的线程可能抢先改掉条件,二因存在虚假唤醒(spurious wakeup);
 *   - signal vs broadcast:讲义强调「想清楚之前 broadcast 更稳妥」,但不说「signal 永远是错的」——
 *     单一等待类型且唤醒任一个都能推进时 cond_signal 更省(正确性优先于性能的取舍);
 *   - 「99% 的并发问题都能用生产者-消费者解决」是讲义原话,保留「差不多/九成九」的口气,不绝对化;
 *   - 条件变量必须与互斥锁配合使用,查/改共享状态都在锁内——不说「条件变量能单独用」。
 */
import type { DemoLine, Topic } from '../../../types';
import type { SelfTestItem } from '../../selfTest';

export const osCondvarTopic: Topic = {
  topicId: 'os-condvar',
  title: '并发控制：条件变量',
  course: '操作系统原理',
  tagline: '让线程排好先来后到',
  transferHint: '餐厅里厨房做菜、服务员端菜——菜没做好服务员就在旁边干等,菜一好厨房吼一嗓子招呼人来端',
  checklist: [
    {
      id: 'c1',
      point: '同步管先后,互斥管独占',
      groundTruth:
        '上一讲的互斥解决的是「不许两个线程同时动同一份共享数据」——它保证 A、B 不重叠,' +
        '但只能给出「A→B 或 B→A」中的某一种,给不了「必须 A 先于 B」这种确定的先后。' +
        '同步是并发控制里除互斥之外的另一个基本问题:控制事件发生的先后顺序,即建立 happens-before 关系——' +
        '等到某个条件成立才继续往下走;若条件还不成立,一定有另一个线程的动作最终把它促成。' +
        '到达「条件成立」的那一刻就是一个全局同步点。',
      keywords: [
        ['先后', '顺序'],
        ['等', '成立'],
        ['谁先', '谁后'],
        ['铁板钉钉'],
      ],
      terms: ['同步', 'happens-before'],
      level: 'L1',
      lookupCard:
        '**同步:管的是「谁先谁后」**\n\n上一讲的**互斥**只保证「不许同时动」——它让 A、B 不撞车,' +
        '却只能落成「要么 A 先、要么 B 先」,保证不了「必须 A 先、B 后」。\n\n' +
        '**同步**是并发控制里另一个基本问题:控制事件的**先后顺序**,也就是建立 **happens-before** 关系——' +
        '等某个条件成立了才继续;条件还没成立,就一定有另一个线程的动作最后来把它促成。\n\n' +
        '到达「条件成立」那一刻,就是一个大家都认账的**同步点**。一句话:互斥管独占,同步管先后。',
      probeLine:
        '老师,「不许两个人同时动」我懂了,可有时候我要的是「必须你先做完、我才能做」,' +
        '这种非得排个先来后到的事,又是靠什么管的呀?',
    },
    {
      id: 'c2',
      point: '别自旋,躺下等叫醒',
      groundTruth:
        '条件不满足时,最笨的办法是自旋:上锁查条件、解锁、不成立就再来一遍,反复空转白烧处理器。' +
        '更好的办法是让操作系统提供 cond_wait:线程查到条件还不成立就调用它,' +
        'cond_wait 会「原子地释放锁并进入等待」——把手里的锁放开、同时让线程立刻睡下,这两件事一步完成;' +
        '被别的线程 cond_signal / cond_broadcast 唤醒后,cond_wait 会重新拿回锁再返回。' +
        '这样等待的线程不占处理器,直到条件真的可能成立才被叫醒。',
      keywords: [
        ['自旋'],
        ['cond_wait'],
        ['放开', '躺下'],
        ['歇着', '喊'],
      ],
      terms: ['cond_wait', '自旋'],
      level: 'L2',
      lookupCard:
        '**别傻等:躺下,等人叫醒**\n\n条件不满足时一遍遍空转查(**自旋**),白白烧着处理器:\n\n' +
        '```c\ndo {\n  mutex_lock(&lk);\n  cond = check();\n  mutex_unlock(&lk);\n} while (!cond);   // 忙等,浪费\n```\n\n' +
        '更省劲的是让操作系统搭把手——**cond_wait**:查到条件还不成立就调用它,' +
        '它会**原子地「放开锁 + 立刻睡下」**(这两件事一步完成),被 signal/broadcast 唤醒后再**重新拿回锁**返回。\n\n' +
        '就像餐厅点了餐:不用守在取餐口盯着,坐下歇着,菜一好服务员会来喊你——又省心又不占地方。',
      probeLine:
        '老师,那我要是必须等一个事儿成了才能往下做,总不能一直站那儿反复问「好了没好了没」吧?' +
        '这么干多累呀,有没有省劲点的法子?',
    },
    {
      id: 'c3',
      point: '万能同步模板',
      groundTruth:
        '条件变量的正确用法是一段固定模板,任何同步都能往里套。等待方:先上锁,用 while 反复查同步条件,' +
        '不成立就 cond_wait 睡下(醒来回到 while 再查一遍);条件成立且持有锁时才继续,最后解锁。' +
        '唤醒方:上锁,修改可能使条件成立的共享状态,再 cond_broadcast 招呼所有等待者,最后解锁。' +
        '锁保证查条件、改条件都在临界区里进行;想清楚「继续执行的条件是什么」,剩下的照抄这段模板即可。',
      keywords: [
        ['模板'],
        ['while', '查条件'],
        ['cond_broadcast'],
        ['反复查', '没到'],
      ],
      terms: ['cond_broadcast', '临界区'],
      level: 'L3',
      lookupCard:
        '**万能同步模板(记住这段代码)**\n\n等待方:\n\n' +
        '```c\nmutex_lock(&lk);\nwhile (!sync_cond()) {     // 反复查条件\n  cond_wait(&cv, &lk);     // 不成立就睡下\n}\n' +
        '// 此时 sync_cond() 成立且持有锁\nmutex_unlock(&lk);\n```\n\n唤醒方:\n\n' +
        '```c\nmutex_lock(&lk);\n// 修改可能使 sync_cond() 成立的共享状态\ncond_broadcast(&cv);       // 招呼所有等待者\nmutex_unlock(&lk);\n```\n\n' +
        '查条件、改条件都在**临界区**里做;想清楚「继续的条件是什么」,剩下的照着 **cond_broadcast** 这段抄。',
      probeLine:
        '老师,道理我懂了,可真到动手写的时候,我到底该照着什么固定套路来,才不会写出岔子呀?',
    },
    {
      id: 'c4',
      point: '醒来要用 while 重查,不用 if',
      groundTruth:
        '等待条件必须用 while 循环反复查,而不能用 if 只查一次。因为「被唤醒」不等于「条件已成立」:' +
        '一是可能有别的线程抢在你前头,把刚成立的条件又用掉了(比如两个消费者被同时叫醒去取同一份数据,' +
        '手快的先取走,后者扑空);二是存在虚假唤醒(spurious wakeup),线程可能在没有 signal 的情况下被唤醒。' +
        '所以醒来后要重新查条件,不成立就接着 cond_wait 睡回去——while 天然做到「醒来即回到判断」,if 做不到。',
      keywords: [
        ['被叫醒', '不等于'],
        ['重新', '再查'],
        ['while', '只查一次'],
        ['虚假唤醒'],
      ],
      terms: ['虚假唤醒'],
      level: 'L3',
      lookupCard:
        '**为什么是 while,不是 if**\n\n「被唤醒」≠「条件成立」,原因有两条:\n\n' +
        '1. **别人抢先用掉了**——两个消费者被同时叫醒去取同一份数据,手快的先取走,慢的醒来发现空的;\n' +
        '2. **虚假唤醒**(spurious wakeup)——线程可能在没人 signal 的情况下莫名醒来。\n\n' +
        '所以醒来必须**重新查一遍**,不成立就接着睡回去:\n\n' +
        '```c\nwhile (!sync_cond())      // ✅ 醒来即回到判断\n  cond_wait(&cv, &lk);\n// if (!sync_cond()) …    ❌ 只查一次,醒来直接往下冲\n```\n\n' +
        '把 while 换成 if,是并发里最经典的坑之一。',
      probeLine:
        '老师,那我被人叫醒之后,是不是就能放心大胆接着往下做了?还用再操心啥吗?',
    },
    {
      id: 'c5',
      point: '生产者-消费者:招牌应用',
      groundTruth:
        '生产者-消费者是最经典的同步问题:双方共用一个缓冲区,生产者放数据(满了就等)、消费者取数据(空了就等),' +
        '要求同一个对象的生产必须 happens-before 它的消费。讲义原话:差不多 99% 的实际并发问题都能归约成它' +
        '(Master-slave / 调度器-工人也是它的变体)。解题套路只有一步:想清楚「什么时候能生产、什么时候能消费」' +
        '这两个继续条件,剩下的直接套万能模板。用打印括号做最小模型:生产=打印左括号、消费=打印右括号,' +
        '同步条件就是「嵌套深度不超过缓冲区大小,且不打出不匹配的括号」。',
      keywords: [
        ['生产者', '消费者'],
        ['缓冲区', '满'],
        ['能放', '能取'],
        ['九成九', '并发'],
      ],
      terms: ['生产者', '消费者', '缓冲区'],
      level: 'L5',
      lookupCard:
        '**生产者-消费者:同步的招牌题**\n\n双方共用一个**缓冲区**:**生产者**放数据(满了就等),' +
        '**消费者**取数据(空了就等),要求同一个对象「生产 happens-before 消费」。\n\n' +
        '讲义原话:差不多 **99%** 的实际并发问题都能归约成它(调度器-工人也是它的变体)。\n\n' +
        '解题只一步:想清楚「什么时候**能放**、什么时候**能取**」这两个继续条件,剩下的照抄万能模板。\n\n' +
        '最小模型——打印括号:生产 = 打印 `(`,消费 = 打印 `)`,同步条件是「嵌套深度不超过缓冲区大小、' +
        '不打出不匹配的括号」。',
      probeLine:
        '老师,这套讲究听着挺玄乎,它到底能用来干点啥实在的事呀?有没有那种一学就能上手的经典例子?',
    },
  ],
  misconceptions: [
    {
      mcId: 'os_condvar_M1',
      topicId: 'os-condvar',
      belief: 'cond_wait 就是先把锁解开、再自己去睡觉这两步分开做,和自己写 unlock() 再 sleep() 是一回事',
      triggerLine:
        '哦——我懂了!那这个 cond_wait,说白了不就是先把锁一松、然后自己躺下睡一觉这两步嘛?' +
        '那我自己写「先解锁,再 sleep 一会儿」不也一样嘛,何必专门用它呢,对吧?',
      correctionCriteria: [
        '指出 cond_wait 把「释放锁」和「进入等待」做成一个不可分割的原子动作,一瞬间同时完成',
        '说明拆成「先解锁、再睡下」中间有缝隙:别人正好在缝隙里改好条件并唤醒,这个唤醒就被丢掉(丢失唤醒),线程可能永远睡下去',
        '(可选)所以必须由操作系统提供 cond_wait 来保证这两步原子,还负责醒来后重新拿回锁',
      ],
      correctionKeywords: [
        ['原子'],
        ['缝隙'],
        ['一瞬间', '同时'],
        ['丢失', '唤醒'],
      ],
      adoptionKeywords: [
        ['分两步'],
        ['一回事'],
        ['先解锁', '再自己睡'],
      ],
      injectAfterChecklist: ['c2'],
      probe: {
        statement:
          'cond_wait 就是先把锁解开、再自己去睡觉这两步分开做,和自己写 unlock() 再 sleep() 是一回事。',
        isTrue: false,
        explanation:
          'cond_wait 是「原子地释放锁并进入等待」——两件事一瞬间同时完成、中间插不进任何人;' +
          '拆成 unlock() 再 sleep(),中间那道缝隙里别人的唤醒会被丢掉(丢失唤醒),线程可能永远睡下去。',
      },
      remedy: {
        microLesson: {
          title: '原子的一步:不给唤醒留缝隙',
          body:
            '把「拆成两步」的危险摆出来看一眼,就明白 cond_wait 为什么必须是原子的:\n\n' +
            '```\n错误写法(拆成两步):\n  线程A: 查到条件不成立 → mutex_unlock(&lk);\n' +
            '  ——就在这一瞬的缝隙里——\n  线程B: mutex_lock(&lk); 改好条件; cond_signal(&cv);  ← 这一嗓子白喊了\n' +
            '  线程A: sleep();   // A 这才躺下,可再没人来叫它了 → 永远睡下去\n```\n\n' +
            '毛病就出在「解锁」和「睡下」之间裂开的那道缝隙:B 的唤醒发生在 A 还没睡下的时候,' +
            '就被彻底丢掉了——这叫**丢失唤醒**(lost wakeup)。\n\n' +
            'cond_wait 的做法是把这两件事捏成**一个原子动作**:一瞬间同时「放开锁 + 进入等待」,' +
            '中间谁也插不进来,任何唤醒要么在它睡下前(那 while 会再查到)、要么在它睡下后(那就能叫醒它),' +
            '绝不会掉进缝隙。醒来时它还会替你重新拿回锁再返回。\n\n' +
            '所以「cond_wait = unlock + sleep」是错的:少了「原子」二字,就少了整个正确性。',
          askBack:
            '下次小白再说「cond_wait 不就是先解锁再自己睡两步嘛」,你打算用哪段「缝隙里丢唤醒」的时序让它看见危险?',
        },
        predictionQuiz: [
          {
            id: 'r1-1',
            code: 'A: 查到不成立 → unlock;\n   —缝隙—\nB: lock; 改好条件; signal;\nA: sleep();',
            question: '照这个「先解锁、再睡下」的拆法,线程 A 最后会怎样?',
            options: [
              '正常被 B 唤醒,继续执行',
              '可能永远睡下去——B 的唤醒发生在 A 还没睡下时,被丢掉了(丢失唤醒)',
              '直接报错崩溃',
              'A 会自动重试直到成功',
            ],
            answerIndex: 1,
            explanation: '唤醒掉进了「解锁」到「睡下」之间的缝隙:B 喊的时候 A 还没睡,等 A 睡下再没人叫——丢失唤醒。',
            checklistRef: 'c2',
            mcRef: 'os_condvar_M1',
          },
          {
            id: 'r1-2',
            question: 'cond_wait(&cv, &lk) 被调用的那一刻,原子地做了哪两件事?',
            options: [
              '加锁 + 打印日志',
              '释放锁 + 立刻进入等待(两件事一步完成,中间不可打断)',
              '只是睡一会儿,锁一直攥在手里',
              '把条件改成真,再返回',
            ],
            answerIndex: 1,
            explanation: 'cond_wait = 原子地「放开锁 + 进入等待」;正因为原子,唤醒才不会掉进缝隙里丢掉。',
            checklistRef: 'c2',
            mcRef: 'os_condvar_M1',
          },
          {
            id: 'r1-3',
            question: '一个在 cond_wait 上睡着的线程被唤醒后,cond_wait 返回之前还会替它做什么?',
            options: [
              '什么都不做,直接往下跑',
              '重新拿回(acquire)那把锁,再从 cond_wait 返回',
              '把锁彻底销毁',
              '再睡一会儿凑够时间',
            ],
            answerIndex: 1,
            explanation: '睡下时放了锁,醒来自然要先重新抢回锁才能安全地回到 while 里查条件——这一步 cond_wait 帮你做了。',
            checklistRef: 'c3',
            mcRef: null,
          },
        ],
      },
    },
    {
      mcId: 'os_condvar_M2',
      topicId: 'os-condvar',
      belief: '唤醒等待线程永远用 cond_signal 叫醒一个就够了,cond_broadcast 叫醒一大群纯属浪费',
      triggerLine:
        '不过老师我又冒出个新主意:既然改完状态就得喊人,那每次用 cond_signal 只叫醒一个不就够了嘛?' +
        '反正总会一个接一个轮到。何必用 cond_broadcast 把一大群全喊起来,那么吵那么费劲,纯属浪费,对吧?',
      correctionCriteria: [
        '指出多类线程等在同一个条件变量上时,signal 只叫一个可能「叫错人」:叫醒了同类,它一查条件不满足又睡回去',
        '说明该醒的没被叫、真正能推进的没醒,严重时会一起睡死',
        '(留余地)只有单一等待类型、且唤醒任一个都能推进时 cond_signal 才够省;想清楚之前 broadcast 更稳妥',
      ],
      correctionKeywords: [
        ['叫错人'],
        ['broadcast', '稳妥'],
        ['睡死'],
        ['不是', '浪费'],
      ],
      adoptionKeywords: [
        ['只叫一个', '够'],
        ['一个接一个', '轮'],
        ['太吵', '浪费'],
      ],
      injectAfterChecklist: ['c1', 'c2', 'c3', 'c4'],
      probe: {
        statement:
          '唤醒等待线程永远用 cond_signal 叫醒一个就够,cond_broadcast 叫醒一群纯属浪费。',
        isTrue: false,
        explanation:
          '多类线程等在同一个条件变量上时,cond_signal 可能叫错人:叫醒了同类,它一查条件不满足又睡回去,' +
          '真正该醒的没被叫,严重时会一起睡死——想省心用 cond_broadcast 让大家各自重查更稳妥;只有单一等待者时 signal 才够。',
      },
      remedy: {
        microLesson: {
          title: '叫错人的代价:signal 与 broadcast',
          body:
            '「叫一个省事」听着有理,可一旦有**好几类**线程等在同一个条件变量上,只 signal 一个就危险了:\n\n' +
            '```\n缓冲区满了。此刻:生产者甲、生产者乙都在等「有空位」,消费者丙在等「有数据」。\n' +
            '  丙取走一个 → 有空位了 → cond_signal(&cv);   // 只叫醒一个\n' +
            '  偏偏叫醒了「生产者乙」的邻座——另一个生产者甲:\n' +
            '  甲醒来一查「有空位?」…可空位刚被想抢的人占了 → 不满足 → 又睡回去\n' +
            '  真正该被叫醒的那个,没人叫 → 大家一起睡死\n```\n\n' +
            '问题在于 cond_signal 叫醒的是「某一个」等待者,你没法保证它就是**能推进**的那个。' +
            '**cond_broadcast** 把所有等待者都喊起来,各自回到 while 重新查一遍条件:能走的走、不能走的再睡——' +
            '多喊几个人只是稍微费点劲,却**不是浪费**,换来的是不会叫错人。\n\n' +
            '留个余地:如果**只有一类**等待者、且唤醒任意一个都能推进,cond_signal 只叫一个确实更省。' +
            '讲义的分寸是——**想清楚之前,broadcast 更稳妥**;这是把正确性放在性能前面的取舍。',
          askBack:
            '下次小白再说「signal 叫一个就够,broadcast 太浪费」,你打算用「叫错人 → 一起睡死」的哪个场景把它拦下来?',
        },
        predictionQuiz: [
          {
            id: 'r2-1',
            question: '好几类线程等在同一个条件变量上,只用 cond_signal 叫醒一个,最坏会发生什么?',
            options: [
              '一定能叫到该醒的那个',
              '可能叫错人:叫醒的同类一查条件不满足又睡回去,该醒的没醒,严重时一起睡死',
              '系统自动改用 broadcast',
              '被叫醒的线程会帮忙转达',
            ],
            answerIndex: 1,
            explanation: 'signal 叫醒的是「某一个」,你无法保证它能推进;叫错人就可能让真正该走的线程一直没人叫。',
            checklistRef: 'c5',
            mcRef: 'os_condvar_M2',
          },
          {
            id: 'r2-2',
            question: 'cond_broadcast 相比只 signal 一个,稳在哪里?',
            options: [
              '它保证只唤醒正确的那一个',
              '把所有等待者都喊起来各自重查条件:能走的走、不能走的再睡,不会漏掉该醒的',
              '它比 signal 执行得更快',
              '它会自动把条件改成成立',
            ],
            answerIndex: 1,
            explanation: '广播 + 各自 while 重查,是「宁可多喊、不要叫错」的稳妥策略;代价只是多几次无用的醒来。',
            checklistRef: 'c3',
            mcRef: 'os_condvar_M2',
          },
          {
            id: 'r2-3',
            question: '那 cond_signal 是不是就永远不该用?',
            options: [
              '对,任何时候都该用 broadcast',
              '不是——只有一类等待者、且唤醒任意一个都能推进时,signal 只叫一个更省',
              '对,signal 是被淘汰的旧接口',
              '看心情,随便用哪个都行',
            ],
            answerIndex: 1,
            explanation: '这是正确性与性能的取舍:能确定「叫谁都行」时 signal 更高效;分不清就 broadcast 稳妥——别把它说死。',
            checklistRef: 'c5',
            mcRef: null,
          },
        ],
      },
    },
    {
      mcId: 'os_condvar_M3',
      topicId: 'os-condvar',
      belief: '既然是被 signal 唤醒的,就说明条件一定成立了,用 if 判断一次就够,不必用 while 循环反复查',
      triggerLine:
        '哦——我懂了!既然是被人喊醒的,那不就说明条件肯定已经成立了嘛,人家总不会没事乱喊我。' +
        '那我用 if 判一次就够了呀,干嘛还费劲用 while 一遍遍查,多此一举,对吧?',
      correctionCriteria: [
        '指出「被唤醒」不代表「条件成立」:可能有别的线程抢先把刚成立的条件又用掉了',
        '说明还存在虚假唤醒:线程可能在没有 signal 的情况下被唤醒',
        '所以必须用 while 每次醒来都重新查,不成立就再睡回去;if 只查一次做不到',
      ],
      correctionKeywords: [
        ['不能', 'if'],
        ['while', '重新'],
        ['不代表', '成立'],
        ['再睡回去'],
      ],
      adoptionKeywords: [
        ['if', '一次就够'],
        ['喊醒', '成立'],
        ['多此一举'],
      ],
      injectAfterChecklist: ['c4'],
      probe: {
        statement:
          '既然是被 signal 唤醒的,就说明条件一定成立,用 if 判断一次就够,不必用 while。',
        isTrue: false,
        explanation:
          '「被唤醒」不代表「条件成立」:可能有别的线程抢先把刚成立的条件用掉了,还可能是虚假唤醒;' +
          '所以必须用 while 每次醒来都重新检查,不成立就再睡回去——if 只查一次做不到这件事。',
      },
      remedy: {
        microLesson: {
          title: 'while 不是 if:醒来先重新验一遍',
          body:
            '「被叫醒 = 条件成立」这个直觉,一遇上抢跑就崩:\n\n' +
            '```\n缓冲区里只有 1 份数据。消费者丙、消费者丁都在等「有数据」。\n' +
            '  生产者放入 1 份 → cond_broadcast → 丙、丁都被叫醒\n' +
            '  丙先抢到锁:查「有数据?」成立 → 取走那一份 → 缓冲区又空了\n' +
            '  丁随后拿到锁:若用 if,它「已经被叫醒过」就直接往下取 → 取了个空!(bug)\n' +
            '           若用 while,它重新查「有数据?」→ 不成立 → 乖乖再睡回去 ✅\n```\n\n' +
            '「被唤醒」只是「条件也许成立了,去看看」,不是「条件一定成立」。原因有两条:\n\n' +
            '1. **有人抢先用掉了**——被同时叫醒的两个线程,手快的先把条件消费掉,慢的就扑空;\n' +
            '2. **虚假唤醒**(spurious wakeup)——线程可能在没人 signal 的情况下莫名醒来,这是允许的。\n\n' +
            '所以模板里雷打不动是 **while**:每次醒来都重新查一遍,不成立就接着 cond_wait 睡回去。' +
            '把 while 写成 if,是并发代码里最经典的坑之一——平时跑得好好的,一到多线程抢跑就出鬼。',
          askBack:
            '下次小白再说「被叫醒就说明条件成立、用 if 就行」,你打算用「两个消费者抢一份数据」怎么演示 if 的翻车?',
        },
        predictionQuiz: [
          {
            id: 'r3-1',
            code: '缓冲区仅 1 份数据\n丙、丁都在等「有数据」\n生产者放入 → broadcast → 丙丁都醒\n丙先取走那一份',
            question: '轮到丁拿锁时,它用 if 只查一次会怎样?',
            options: [
              '正常取到一份数据',
              '直接往下取,却取了个空——数据刚被丙拿走了(用 while 就能再睡回去)',
              '自动等丙放回来',
              '触发系统报警',
            ],
            answerIndex: 1,
            explanation: '丁「被叫醒过」不代表「还有数据」:if 不重查就扑空;while 会重新查到「空了」而乖乖再睡。',
            checklistRef: 'c4',
            mcRef: 'os_condvar_M3',
          },
          {
            id: 'r3-2',
            question: '为什么等待条件要用 while 而不是 if?',
            options: [
              'while 写起来更短',
              '被唤醒不等于条件成立:可能被抢先用掉、也可能虚假唤醒,必须每次醒来重新查',
              'if 在 C 语言里不能和 cond_wait 搭配',
              '纯属编码习惯,两者等价',
            ],
            answerIndex: 1,
            explanation: 'while 让「醒来即回到判断」,不成立就再睡;这是条件变量正确性的关键,不是风格问题。',
            checklistRef: 'c4',
            mcRef: 'os_condvar_M3',
          },
          {
            id: 'r3-3',
            question: '「虚假唤醒(spurious wakeup)」指的是?',
            options: [
              '线程假装被唤醒,其实还在睡',
              '线程可能在没人 signal/broadcast 的情况下也被唤醒——所以醒来必须重新查条件',
              '唤醒信号在网络里丢了',
              '操作系统故意骗线程玩',
            ],
            answerIndex: 1,
            explanation: '规范允许线程无缘无故醒来;正因如此,while 重查是必须的,不能假设「醒了就一定有人叫且条件成立」。',
            checklistRef: 'c4',
            mcRef: null,
          },
        ],
      },
    },
  ],
  quizBank: [
    {
      id: 'q1',
      question: '同步和互斥,各自解决什么问题?',
      options: [
        '两个是一回事,只是叫法不同',
        '互斥保证「不许同时动」;同步控制「事件的先后顺序」(happens-before)——等条件成立才继续',
        '互斥管先后,同步管独占',
        '同步只在单线程里才有意义',
      ],
      answerIndex: 1,
      explanation: '互斥让 A、B 不重叠却给不了确定先后;同步专管「必须 A 先于 B」,靠等待某个条件成立来实现。',
      checklistRef: 'c1',
      mcRef: null,
    },
    {
      id: 'q2',
      question: 'cond_wait(&cv, &lk) 被调用的那一刻,原子地做了哪两件事?',
      options: [
        '先解开锁,再单独去睡——和自己写 unlock() 加 sleep() 一样',
        '原子地「释放锁 + 进入等待」,两件事一步完成、中间不可打断',
        '把条件改成真,然后返回',
        '一直攥着锁睡觉,等人来叫',
      ],
      answerIndex: 1,
      explanation: '正因为「放锁 + 睡下」是原子的,别人的唤醒才不会掉进缝隙被丢掉(丢失唤醒);醒来它还会重新拿回锁。',
      checklistRef: 'c2',
      mcRef: 'os_condvar_M1',
    },
    {
      id: 'q3',
      question: '万能同步模板里,等待方的正确写法是?',
      options: [
        '上锁 → if (!条件) cond_wait → 解锁',
        '上锁 → while (!条件) cond_wait → 解锁(醒来回到 while 再查一遍)',
        '不用上锁,直接 cond_wait 等着',
        '先 cond_wait 睡下,再上锁查条件',
      ],
      answerIndex: 1,
      explanation: '固定套路:上锁、while 反复查条件、不成立就 cond_wait;查条件必须在锁里,循环用 while 不用 if。',
      checklistRef: 'c3',
      mcRef: null,
    },
    {
      id: 'q4',
      question: '等待条件为什么必须用 while 循环,而不能用 if 只查一次?',
      options: [
        '为了让代码更长更严谨',
        '「被唤醒」不等于「条件成立」:可能被别人抢先用掉、也可能虚假唤醒,必须每次醒来重新查',
        'if 在多线程里语法不合法',
        '因为 while 执行得更快',
      ],
      answerIndex: 1,
      explanation: '两个线程被同时叫醒抢一份数据,手快的先取走;用 if 的那个不重查就扑空——while 会重新查到并再睡回去。',
      checklistRef: 'c4',
      mcRef: 'os_condvar_M3',
    },
    {
      id: 'q5',
      question: '多类线程等在同一个条件变量上,唤醒时该优先用哪个?为什么?',
      options: [
        '永远用 cond_signal,叫一个最省',
        '想清楚之前优先 cond_broadcast:只 signal 一个可能叫错人,该醒的没醒、甚至一起睡死',
        '两个完全等价,随便用',
        '谁都别用,让线程自己醒',
      ],
      answerIndex: 1,
      explanation: 'signal 叫的是「某一个」,叫错同类就会让真正能推进的没被叫;broadcast 全喊起来各自重查更稳妥(单一等待者时 signal 才够省)。',
      checklistRef: 'c5',
      mcRef: 'os_condvar_M2',
    },
    {
      id: 'q6',
      question: '条件不满足时,为什么不推荐自旋(反复空转查),而改用 cond_wait?',
      options: [
        '自旋会把变量算错',
        '自旋反复空转白烧处理器;cond_wait 让线程躺下不占处理器,被唤醒才醒来',
        'cond_wait 比自旋更容易死锁',
        '自旋在 C 语言里写不出来',
      ],
      answerIndex: 1,
      explanation: '忙等(自旋)持续占用处理器却什么也没干成;cond_wait 把等待的线程挂起,直到条件可能成立才唤醒——省电又省算力。',
      checklistRef: 'c2',
      mcRef: null,
    },
  ],
  prep: {
    microLecture: {
      title: '五分钟看懂:让线程排好先来后到',
      body:
        '把这一讲想成「给线程排队、讲清谁先谁后」,五个要点就串起来了:\n\n' +
        '1. **同步管先后,互斥管独占**。上一讲的互斥只保证「不许同时动」,给不了「必须你先我后」;' +
        '同步专门控制事件的先后顺序(happens-before)——等一个条件成立才继续。\n' +
        '2. **别自旋,躺下等叫醒**。条件不满足时一遍遍空转查(自旋)白烧处理器;更好的办法是 cond_wait:' +
        '它原子地替你放开锁、让你睡下,等别人唤醒再重新拿回锁。\n' +
        '3. **万能同步模板**。等待方:上锁 → while 查条件 → 不成立就 cond_wait;' +
        '唤醒方:上锁 → 改共享状态 → cond_broadcast → 解锁。记住这段代码,任何同步都能往里套。\n' +
        '4. **为什么是 while 不是 if**。被叫醒不等于条件成立:可能有人抢先把条件用掉了,还可能虚假唤醒;' +
        '所以每次醒来都要重新查,不成立再睡回去。\n' +
        '5. **生产者-消费者**。想清楚「什么时候能放、什么时候能取」,剩下的套模板——' +
        '差不多九成九的并发问题都能归到它。\n\n' +
        '判断口诀:**先后靠同步,等待用 cond_wait,查条件用 while,唤醒用 broadcast**。' +
        '小白最容易问倒你的地方:「cond_wait 不就是先解锁、再自己睡两步嘛?」\n\n' +
        '**讲课节奏建议**\n\n' +
        '- **先讲①②,把「不傻等」的画面立起来**:①用「必须你先、我后」把同步和互斥分开,' +
        '②用餐厅点餐叫号讲清 cond_wait 让你躺下等叫醒,别死守窗口空转。\n' +
        '- **②讲完,警报拉响**:小白八成会说「那不就是先解锁、再自己睡两步嘛」。先明确否定,' +
        '再讲原子性:放锁和睡下捏成一个不可分割的动作;要是拆成两步,中间那道缝隙里别人的唤醒会丢掉、' +
        '线程睡死——这叫丢失唤醒。纠正站稳了,顺势把③万能模板讲透。\n' +
        '- **中场把「找条件 → 套模板」串一遍**再进④:上锁、while 查、不成立就等,小白复述对了再走。\n' +
        '- **讲完④,它还会来一次**:「被叫醒不就说明条件成立了嘛,用 if 一次就够了吧?」' +
        '用「两个消费者抢一份数据」顶回去;接着讲⑤生产者-消费者,末尾它多半还嘀咕' +
        '「signal 叫一个就够、broadcast 太浪费」——用「叫错人会睡死」收尾。\n\n' +
        '**一句话收束**\n\n' +
        '**同步就是让线程排好先来后到:想清楚继续的条件,上锁后 while 查、不成立就 cond_wait 睡下,' +
        '改了共享状态就 broadcast 把大家叫醒重查——这套模板万能。**\n\n' +
        '**再深一锹(选读)**\n\n' +
        '- **锁和条件变量为什么总成对**。查条件、改条件都在碰共享状态,必须在锁里做;' +
        'cond_wait 之所以要「原子地放锁 + 睡下」,正是为了不在「查完」到「睡下」之间漏掉别人的唤醒。\n' +
        '- **signal 也有它的位置**。只有一类等待者、且唤醒任一个都能推进时,cond_signal 只叫一个更省;' +
        '分不清就 broadcast 稳妥——这是正确性优先于性能的取舍,不是「signal 永远错」。\n' +
        '- **计算图与调度器**。给每个计算节点配一个条件变量,或用「一个调度器派活、一群工人干活」的模式,' +
        '就能在任意有向无环图(DAG)上并行——课上说,几乎所有并行计算都能这么看。\n' +
        '- **括号练手**。把生产写成打印「(」、消费写成「)」,同步条件就是「嵌套深度别超过缓冲区大小、' +
        '别打出不匹配的括号」——一个能亲手跑的最小同步模型。',
    },
    examples: [
      {
        title: '例 1:万能同步模板(记住这段就够)',
        code:
          '// 等待方\n' +
          'mutex_lock(&lk);\n' +
          'while (!sync_cond()) {      // 反复查条件,不用 if\n' +
          '  cond_wait(&cv, &lk);      // 原子地放锁+睡下,醒来重新拿回锁\n' +
          '}\n' +
          '// 此时 sync_cond() 成立且持有锁\n' +
          'mutex_unlock(&lk);\n\n' +
          '// 唤醒方\n' +
          'mutex_lock(&lk);\n' +
          '/* 修改可能使 sync_cond() 成立的共享状态 */\n' +
          'cond_broadcast(&cv);        // 招呼所有等待者\n' +
          'mutex_unlock(&lk);',
        walkthrough:
          '整讲的骨架就这两段。三处别漏:查条件、改条件都在锁里(临界区);等待用 while 不用 if;' +
          '唤醒想清楚之前先用 broadcast。想同步任何东西,只要把 sync_cond() 换成你的「继续条件」即可。',
      },
      {
        title: '例 2:拆成两步 → 丢失唤醒(反面对照)',
        code:
          '// ❌ 错误:把 cond_wait 拆成「先解锁、再睡下」\n' +
          'A: mutex_lock(&lk);\n' +
          '   if (!cond) { mutex_unlock(&lk);   // 解锁\n' +
          '                /* —— 缝隙 —— */\n' +
          '                sleep(); }            // 才睡下\n' +
          '// 就在缝隙里:\n' +
          'B: mutex_lock(&lk); cond = 1; cond_signal(&cv); mutex_unlock(&lk);',
        walkthrough:
          'B 的唤醒发生在 A「已解锁、还没睡下」的缝隙里,就被彻底丢掉了——A 随后睡下,再没人来叫,' +
          '可能永远睡下去(丢失唤醒)。cond_wait 把「放锁 + 睡下」做成一个原子动作,正是为了堵死这道缝隙。' +
          '别把它讲成「一定死锁」——准确说法是「可能丢唤醒、可能永远睡下去」。',
      },
      {
        title: '例 3:生产者-消费者(打印括号最小模型)',
        code:
          '// 生产 = 打印 "(",消费 = 打印 ")";缓冲区大小 n,depth = 当前嵌套深度\n' +
          'void T_producer() {\n' +
          '  mutex_lock(&lk);\n' +
          '  while (!(depth < n)) cond_wait(&cv, &lk);  // 满了就等\n' +
          '  depth++; printf("(");\n' +
          '  cond_broadcast(&cv);\n' +
          '  mutex_unlock(&lk);\n' +
          '}\n' +
          '// 消费者对称:while (!(depth > 0)) …; depth--; printf(")");',
        walkthrough:
          '继续条件一眼看清:能生产当且仅当 depth < n(缓冲区没满),能消费当且仅当 depth > 0(有数据)。' +
          '写清这两个条件,剩下的照抄模板。注意这里用 broadcast:若换成 signal,可能把等着的另一个生产者叫醒、' +
          '它一查还是满的又睡回去——课上标注的 ⚠️ 正是这个坑。',
      },
    ],
    selfCheck: [
      '能一句话说清同步和互斥的分工吗?(互斥管独占「不许同时」,同步管先后「必须谁先谁后」)',
      '为什么不推荐自旋、改用 cond_wait?cond_wait 被调用那一刻,原子地做了哪两件事?',
      '万能模板的等待方和唤醒方能默写吗?等待为什么必须用 while、不能用 if?',
      '小白坚持「cond_wait 就是先解锁再自己睡两步」,你的「缝隙里丢唤醒」反驳时序想好了吗?',
      'cond_signal 和 cond_broadcast 什么时候用哪个?为什么讲义说「想清楚之前 broadcast 更稳妥」?',
    ],
    taskCard:
      '📋 你的教学任务:等会小白会问你——「cond_wait 是不是就是先把锁解开、再自己去睡一觉这两步,' +
      '跟我自己写 unlock() 再 sleep() 一回事?」带着这个问题去读下面的材料,想好你打算怎么给它讲明白。' +
      '纠不动它,它会开心地把错的学走。',
    references: [
      {
        title: '并发控制：同步与条件变量',
        url: 'https://jyywiki.cn/OS/2026/lect15.md',
        kind: '讲义',
        note:
          'jyy 2026 春第 15 讲讲义:从「同步 = 建立 happens-before」讲到 cond_wait/signal/broadcast 万能模板、' +
          '生产者-消费者与计算图;本讲所有关键说法(原子等待、while 重查、broadcast 更稳妥)的原始出处。',
      },
      {
        title: '15 - 并发控制：条件变量和万能同步方法 [2026 南京大学操作系统原理]',
        url: 'https://www.bilibili.com/video/BV1t3dQBAEzd',
        kind: '视频',
        note:
          'UP 主「绿导师原谅你了」(jyy 官方号):第 15 讲课堂实录——乐团同步、生产者-消费者括号练习和' +
          'cond_signal 的 ⚠️ 现场演示,看视频比读讲义更能体会「叫错人」的危险。',
      },
      {
        title: 'Operating Systems: Three Easy Pieces',
        url: 'https://pages.cs.wisc.edu/~remzi/OSTEP/',
        kind: '教程',
        note:
          '课程指定教科书 OSTEP 官网,全书 PDF 免费:本讲对应第 30 章 Condition Variables,' +
          '中译本《操作系统导论》;书里把「为什么用 while 不用 if」「signal vs broadcast」讲得极细,' +
          '并配有生产者-消费者的完整推演。',
      },
      {
        title: 'pthread_cond_signal(3p) - Linux manual page',
        url: 'https://man7.org/linux/man-pages/man3/pthread_cond_signal.3p.html',
        kind: '官方文档',
        note:
          'POSIX 官方手册:cond_signal 唤醒「至少一个」、cond_broadcast 唤醒「全部」的准确定义,' +
          '以及「虚假唤醒(spurious wakeup)允许发生、故必须在循环里判断谓词」的规范原文——' +
          '课上两个坑的权威依据都在这一页。',
      },
      {
        title: '操作系统原理 (2026 春季学期)',
        url: 'https://jyywiki.cn/OS/2026/',
        kind: '讲义',
        note:
          '课程主页:完整讲次目录、实验(M1-M6)与全部回看入口。条件变量属于「并发」单元,' +
          '建议连着第 14 讲(互斥)和第 16 讲(信号量)一起看,才能看清「互斥→同步→更高层抽象」这条线。',
      },
    ],
  },
};

export const osCondvarDemo: DemoLine[] = [
  {
    label: '① 讲:同步管先后,互斥管独占',
    text:
      '我先说说这一讲要解决的新问题。上一讲讲的互斥,管的是「不许两个人同时去动同一样东西」,' +
      '它能保证大家不撞车、错开来做,可它只能做到「要么你先要么我先」,保证不了「必须你先做、我后做」' +
      '这种铁板钉钉的先后。今天要补的这块新本事叫同步,专管事件的先后顺序:你得等某个条件成立了,' +
      '才能接着往下走;要是条件还没成立,那就一定有另一个人的动作,最后来把它促成。说到底,' +
      '就是要在几个线程之间理出一个稳稳当当的谁先谁后。这道理跟做饭吃饭一样,饭还没做好,' +
      '你就只能在饭桌前干等着,直到有人把饭端上桌,你才动得了筷子。',
    note: '命中 c1(同步 vs 互斥、先后顺序)→ 小白开窍复述,追问 c2(总不能一直站那儿反复问吧)',
  },
  {
    label: '② 讲:别自旋,cond_wait 躺下等叫醒',
    text:
      '那这个「等」,怎么等才聪明呢?最笨的办法叫自旋:上锁查一下条件,不成立就解锁,' +
      '然后反反复复空转着问「到了没到了没」,白白烧着处理器。更省劲的办法是让操作系统搭把手:' +
      '它提供一个叫 cond_wait 的操作——你查到条件还不成立,就调用它,它会替你把手里的锁放开、' +
      '让你立刻躺下歇着;等别人把条件凑齐了、喊你一声,你再重新拿回锁、醒过来接着走,' +
      '压根不用死守在那儿空转。这就好比你在餐厅点了餐,用不着一直守在取餐口盯着,尽管坐下歇着,' +
      '菜一好服务员自然会来喊你,又省心又不占地方。',
    note: '命中 c2 + 金句类比收录(餐厅点餐叫号)→ 触发 M1 注入:「cond_wait 不就是先解锁再自己睡两步?」(高潮)',
  },
  {
    label: '③a 被带偏(演示盲区暴露)',
    text:
      '对哦……好像还真是这么回事,先松开锁、再自己躺下睡,分两步做好像也没啥差别嘛。' +
      '那我以后就记成:cond_wait 说白了就是先解锁、再自己睡一觉,跟我自己写个解锁加睡觉是一回事,' +
      '省得记新词儿。',
    note: 'M1 判定「被带偏」(命中「分两步 / 一回事 / 先解锁+再自己睡」)→ 小白开心地学错了,盲区落档(severity=high),考小白关联题必错',
  },
  {
    label: '③b 正确纠正(对照分支)',
    text:
      '不对,cond_wait 可不是「先解锁、再睡」这么两步分着做的。它把「松开锁」和「躺下等着」' +
      '捏成了一个原子动作,一瞬间同时完成,中间谁也插不进来。你要真拆成两步,这中间就裂开一道缝隙:' +
      '偏偏有人赶在这道缝隙里把条件凑齐、还喊了你一嗓子,可你那会儿还没躺下,这一嗓子就白喊、被丢了,' +
      '等你躺下就再没人叫、睡死过去——这毛病就叫丢失唤醒。所以正确的写法是套一段固定模板:先上锁,' +
      '拿一个 while 反复查条件到了没,没到就 cond_wait 睡下;而改动状态那一方,改完顺手 cond_broadcast ' +
      '招呼所有等着的人。',
    note: 'M1 判定「已纠正」(命中「原子 / 缝隙 / 一瞬间+同时 / 丢失+唤醒」)+ 顺势命中 c3(万能模板)→ Aha 复述后追问 c4,纠错力 +1',
  },
  {
    label: '④ 串讲:找条件—套模板的全景',
    text:
      '我把前面这几块先串一串:首先,同步管的就是事情的先来后到,你得等条件够了才能往下走;' +
      '然后,想「等」得聪明点,别傻乎乎地一遍遍空转着问,而是躺下把锁让出去,等人来叫你;' +
      '最后真写起来,就是一段顺手的套路——上好锁,盯着那个条件,不够就歇着、够了再干活,' +
      '改动的那一方顺手把等着的人都招呼一声。这么一套下来,好几个线程之间就有了稳稳当当的先来后到。',
    note: '复述巩固(无新命中,与主题词汇有交集不算偏题)→ 小白就 c4(被叫醒是不是就能放心往下做)发起边界追问;Lv5 人格在此轮提前发起迁移追问',
  },
  {
    label: '⑤ 讲:醒来要用 while 重查',
    text:
      '再教你一个特别容易踩的讲究:被叫醒,并不等于「条件真的成立了」。所以查条件必须用 while 循环反复查,' +
      '而不能用 if 只查一次。为什么呢?你被人喊起床的那一刻,得重新亲眼再查一遍条件到底成没成——' +
      '很可能有别人抢在你前头,把刚成立的条件又给用掉了(比方俩人被同时叫起来取同一份东西,' +
      '手快的先拿走,你就扑了个空);再说有时候还会莫名其妙地被叫醒,这叫虚假唤醒。所以醒来先重新查,' +
      '不成立就接着 cond_wait 睡回去,才稳妥。',
    note: '命中 c4(while vs if、虚假唤醒)→ 触发 M3 注入(被叫醒不就说明条件成立、用 if 一次就够?)',
  },
  {
    label: '⑥ 纠正 M3 + 讲生产者-消费者',
    text:
      '不对,可不能光凭「被喊醒」就用 if 判一次——被叫醒不代表条件就一定成立,这恰恰是最容易翻车的地方,' +
      '所以必须用 while,每次醒来都重新验一遍,不成立就再睡回去。好,现在把这套东西用到最经典的场景:' +
      '生产者和消费者共用一个缓冲区。生产者往里放东西,缓冲区满了就得等;消费者从里头取东西,空了也得等。' +
      '你只要想清楚「什么时候能放、什么时候能取」这两个条件,剩下的照着刚才那段模板抄就行——' +
      '课上讲,差不多九成九的并发问题,最后都能归到这个生产者-消费者上来解决。',
    note: '纠正 M3(命中「不能+if / while+重新 / 不代表+成立 / 再睡回去」)+ 命中 c5(生产者-消费者)→ 同轮衔接注入 M2(只 signal 一个够了?)',
  },
  {
    label: '⑦ 纠正 M2(收尾)',
    text:
      '还真不能图省事只用 signal。你只叫醒一个,碰上好几类线程都等在同一个门口的时候,很可能叫错人——' +
      '比如你本想喊个消费者,结果喊醒的是另一个生产者,它一看条件没满足,扭头又睡回去了,' +
      '真正该醒的那个反倒没人叫,搞不好大家一起睡死在那儿。所以想省心,改完状态就用 cond_broadcast ' +
      '把等着的人全招呼一遍,让他们各自醒来重新查一次条件——多喊几个人只是稍微费点劲,却稳妥得多,' +
      '绝不是浪费。当然要是只有一类人等着、叫谁都能往下走,signal 叫一个也够省。这么一来,谁该走谁该等,就都对上了。',
    note: '纠正 M2(命中「叫错人 / broadcast+稳妥 / 睡死 / 不是+浪费」)→ 三误区全纠正、五要点全命中,可下课考小白(预期 100 分出师)',
  },
  {
    label: '卡壳演示(触发 R1 救援)',
    text: '嗯……这块我卡住了,一下子想不起来后面该怎么讲了……',
    note: '卡壳信号 → 小白递台阶(R1);连续两次 → 一起查书(R2)',
  },
  {
    label: '偏题演示(内容围栏)',
    text: '小白,周末学院要办桌游社招新,听说还管一杯奶茶,咱俩要不要一起去凑个热闹呀?',
    note: '偏题 → 小白角色内拉回:「这跟今天的知识点没关系吧」',
  },
];

/**
 * 摸底快测题组(聚合于 src/data/selfTest.ts)。
 * 出题纪律:不与 quizBank / probe 题干重复,只考同一要点的另一侧面;
 * 干扰项须是「听着有道理的常见误解」,每题只留一个略带幽默的。
 * 分寸沿用文件头内容红线:丢失唤醒只说「可能永远睡下去」,signal/broadcast 留「单一等待者时 signal 才够省」的余地。
 */
export const osCondvarSelfTest: SelfTestItem[] = [
  {
    id: 'st1',
    dimension: '概念',
    question: '「食堂窗口一次只让一个人打饭」和「必须等厨师把菜端出来你才能打」,分别更像哪个?',
    options: [
      '前者像互斥(管独占),后者像同步(管先后)',
      '前者像同步,后者像互斥',
      '两个都是互斥,同步用不上',
      '两个都是同步,和互斥无关',
    ],
    answerIndex: 0,
    explanation: '一次只让一个人 = 不许同时(互斥);必须等菜出来才能打 = 谁先谁后的先后顺序(同步)。',
    checklistRef: 'c1',
    mcRef: null,
  },
  {
    id: 'st2',
    dimension: '推演',
    code: '生产 = 打印 "(",消费 = 打印 ")",缓冲区大小 n = 3',
    question: '照「嵌套深度不超过 n、括号必须匹配」的同步条件,下面哪个序列是合法输出?',
    options: [
      '(((()))) —— 一口气叠到第 4 层',
      '((())())( —— 深度始终不超过 3,括号也匹配',
      '())( —— 先冒出一个多余的右括号',
      '((( 之后直接收工 —— 剩三个左括号没闭合',
    ],
    answerIndex: 1,
    explanation: '生产要 depth<n、消费要 depth>0:深度不越过 3、右括号不早于左括号,才是合法的生产-消费序列。',
    checklistRef: 'c5',
    mcRef: null,
  },
  {
    id: 'st3',
    dimension: '边界',
    question: '有人图省事把 cond_wait 手写成「先 unlock(),紧接着 sleep()」。最可能踩的坑是?',
    options: [
      '两次都拿不到锁,程序编译不过',
      '解锁到睡下之间的缝隙里,别人的唤醒被丢掉,线程可能永远睡下去(丢失唤醒)',
      '锁会被复制成两把',
      '睡眠时间算错,早醒了 1 秒',
    ],
    answerIndex: 1,
    explanation: 'cond_wait 必须原子地「放锁 + 睡下」;拆成两步,唤醒掉进缝隙就丢了——这正是它不能自己攒的原因。',
    checklistRef: 'c2',
    mcRef: 'os_condvar_M1',
  },
  {
    id: 'st4',
    dimension: '辨析',
    question: '缓冲区只剩 1 份数据,两个消费者被同时叫醒。若其中一个用 if 只查一次,会怎样?',
    options: [
      '两个都能取到数据,皆大欢喜',
      '手快的取走那一份;用 if 的那个不重查就往下取,扑了个空(用 while 才会再睡回去)',
      '系统自动再生产一份补给它',
      '两个消费者会合并成一个',
    ],
    answerIndex: 1,
    explanation: '「被叫醒」不代表「还有数据」:数据被抢走后,if 不重查就扑空;while 会重新查到「空了」而乖乖再睡。',
    checklistRef: 'c4',
    mcRef: 'os_condvar_M3',
  },
  {
    id: 'st5',
    dimension: '应用',
    question: '接力赛里「后一棒必须接到交接棒才能起跑」,用今天的工具怎么写最贴切?',
    options: [
      '给交接棒上把互斥锁,谁抢到谁跑',
      '后一棒 while(没接到棒) 就 cond_wait 等着;前一棒交棒后 cond_broadcast 叫它',
      '让后一棒自旋空转,一直问「接到没接到没」',
      '把两棒合成一个人跑,免得同步',
    ],
    answerIndex: 1,
    explanation: '「接到棒才能跑」是一个 happens-before:等待方 while+cond_wait,交棒方改状态后 broadcast——正是万能模板。',
    checklistRef: 'c3',
    mcRef: null,
  },
  {
    id: 'st6',
    dimension: '辨析',
    question: '生产者和消费者都等在同一个条件变量上。只用 cond_signal 叫醒一个,最担心的是?',
    options: [
      '叫错人:本该叫消费者,却叫醒另一个生产者,它一查不满足又睡回去,该醒的没醒',
      'signal 会把两个都叫醒,太吵',
      'signal 执行太慢,赶不上趟',
      'signal 会顺手把缓冲区清空',
    ],
    answerIndex: 0,
    explanation: 'signal 叫的是「某一个」,多类线程混等时可能叫错;broadcast 全喊起来各自重查更稳妥(单一等待者时 signal 才够省)。',
    checklistRef: 'c5',
    mcRef: 'os_condvar_M2',
  },
  {
    id: 'st7',
    dimension: '推演',
    question: '两种等待法:A「反复空转查条件」,B「cond_wait 睡下等叫醒」。条件迟迟不满足时,谁更费机器?',
    options: [
      'A 更费:一直空转白占着处理器,啥也没干成',
      'B 更费:睡觉也要耗电',
      '一样费,反正都在等',
      'B 更费:醒来还要重新查一遍',
    ],
    answerIndex: 0,
    explanation: '自旋(A)持续占用处理器却毫无进展;cond_wait(B)把线程挂起、不占处理器,直到被唤醒——所以不推荐忙等。',
    checklistRef: 'c2',
    mcRef: null,
  },
];
