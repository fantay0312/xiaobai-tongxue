# 外观全面审查报告 · 2026-07-06

方法:13 个页面/横切审查代理扫全部 CSS Module + TSX(127 条原始发现 → 逐条对抗验证 → 94 条确认、3 条驳回),叠加浏览器真实渲染巡查(landing/study/prep/classroom/review/growth/teacher/login,dev server 实测)。

## 浏览器实测发现(代码审查之外)

1. **[HIGH] 路由切换不重置滚动位置** — `src/main.tsx` HashRouter 无 ScrollToTop/ScrollRestoration。实测:landing 底部点「进入书斋」到 /study、复盘页跳 /growth,均停留在旧滚动位置,首屏被吸顶导航裁切。修法:App.tsx 挂一个 useLocation+useEffect(window.scrollTo(0,0)) 的 ScrollToTop。
2. **[MED] 演示助手折叠态渗漏** — `classroom.module.css:269` `.demoInner` 的 `padding-bottom: var(--gap-hair)` 在 `grid-template-rows: 0fr` 塌缩后仍占 6px 可视窗口(overflow:hidden 裁的是 padding-box),卡片顶边从缝里漏出一排,被输入区切成碎条。答辩带 ?demo=1 时可见。修法:padding 移到 .demoLine 外围或折叠时置 0。
3. **[MED] 小白 3D 头像加载空窗** — three.js 1.36MB chunk 未分包,/study 首屏右侧空 2-4 秒、讲解舱头像框空约 8 秒才弹出,无骨架/占位(FallbackBlob 只用于 WebGL 不可用)。修法:chunk 加载期间先渲染 FallbackBlob 或占位轮廓。
4. **[MED] 复盘页底部死胡同** — 证据链之后无任何下一步动作(回书斋/再讲一遍/去补学),读完必须靠顶部导航折返;与备课页底部有明确 CTA 的做法不一致。
5. **[LOW] landing 与 /study 首屏 H1 逐字相同**(「不是 AI 教你,是你教会一个 AI」),点完 CTA 进门厅看到同一句主标题,叙事上原地踏步。

## 多代理审查确认发现(94 条,按严重度)


### HIGH

- **src/pages/growth/growth.module.css:171** (responsive) 盲区图谱 SVG 以 960×300 固定 viewBox 整体缩放,375px 手机上节点文字缩到约 5px 完全不可读,节点命中区缩到约 11px 远小于 40px 触控标准
  - 修法: 窄屏改变布点策略而非整体缩放:≤640px 时用更窄的 viewBox(如 480 宽、节点分两行)或给 .mapSvg 设 min-width: 640px 并让 .mapWrap 变为 overflow-x: auto 的滚动容器,保证文字 1:1 渲染;同时把 hover 信息在点击(onSelect)后也展示
- **src/pages/home/shelf.module.css:107** (interaction-state) 书脊入场动画用 fill-mode: both,动画结束后 to 帧的 transform: none 永久压住 :hover/:focus-visible 的抽书位移,hover 只剩阴影不动书。
  - 修法: 把 fill-mode 从 both 改为 backwards(`animation: shelfRise var(--t-med) var(--ease-out) backwards;`):延迟期间仍保持 from 帧(opacity 0)满足 stagger 需求,动画结束后不再钉住 transform,hover 抽书恢复。home.module.css 的 .enter 同理。
- **src/pages/home/shelf.module.css:214** (contrast) 书架「待复习」书脊的状态文字用 --amber 写在 amber-wash 底上,实测对比度仅约 2.08:1(11px 小字),正常使用下几乎不可读。
  - 修法: 为文字场景增加一档深藤黄令牌(如 --amber-ink: oklch(0.52 0.11 80),在 paper 上约 4.6:1),.review .status 与其他 amber 文字改用它;--amber 保留给色块/描边/图形。
- **src/pages/review/review.module.css:554** (contrast) 证据链时间线的 .tagWarn 标签是 amber 文字压 amber-wash 底,对比度约 2.08:1,11px 下基本读不清。
  - 修法: 与书架同用加深一档的 --amber-ink 作为文字色,背景仍用 --amber-wash、描边可留 --amber;顺带把 tagGood/tagBad 的文字色也换成加深档(如 --jade 降 L 至 ≈0.50、--cinnabar 已接近可保留)。

### MEDIUM

- **src/components/Md.tsx:19** (inconsistency) Md 组件(FROZEN 公共件)自身不带任何基础样式,三个消费方各自重写 pre/code 排版且已经漂移,同类内容在不同页面观感不一致。
  - 修法: 为 Md 输出建一个共享基底(如 Md.module.css 提供 pre/code 默认样式,消费方只覆盖场景色),消除三处近重复块和 3px 魔法圆角,统一行内 code 观感。
- **src/components/shell/AppShell.module.css:156** (contrast) 全站顶部导航链接默认色是 --ink-faint,在宣纸底上对比度约 3.63:1,15px 正文字号不达 AA(4.5:1)。
  - 修法: 把 --ink-faint 的 L 从 0.60 降到 ≈0.52(在 paper 上约 4.9:1),一处改令牌全站受益;若想保留极淡层次,可另加 --ink-ghost 专用于纯装饰性文字,交互文字(导航、链接)一律不低于 --ink-soft。
- **src/components/shell/AppShell.module.css:258** (overflow) 已登录用户名没有任何截断限制,较长用户名(如邮箱)会把顶栏撑出视口,产生整页横向滚动条。
  - 修法: 给 .userName 加 max-width: 8em; overflow: hidden; text-overflow: ellipsis; white-space: nowrap,并考虑 .nav 加 min-width: 0。
- **src/components/shell/SettingsDrawer.module.css:198** (focus) 「测试连接」按钮的 :focus-visible 把 outline 去掉后只留 10% 透明度的 azure-wash 光圈,焦点指示在宣纸底上几乎不可见。
  - 修法: 与同文件其他控件统一:outline: 2px solid var(--azure); outline-offset: 2px;,删掉 outline: none + wash 光圈的写法。
- **src/components/shell/SettingsDrawer.module.css:213** (token-violation) 连接失败状态用了硬编码十六进制红 #c0392b,绕过了现成的 --cinnabar 令牌,违反 DESIGN.md「不得新造十六进制色值」硬性禁令。
  - 修法: 改为 color: var(--cinnabar)。
- **src/components/shell/SettingsDrawer.tsx:38** (interaction-state) 设置抽屉打开时没有锁定 body 滚动,滚轮/触摸在遮罩或面板到底后会继续滚动背后的页面。
  - 修法: open 时在 useEffect 里设置 document.body.style.overflow='hidden'(cleanup 恢复),并给 .panel 加 overscroll-behavior: contain。
- **src/index.css:27** (motion) 全局 reduced-motion 规则只压了 duration、漏掉 animation-iteration-count,无限循环动画在减动效模式下会以 0.01ms/圈无限重启,产生闪烁与空转开销。
  - 修法: 在该媒体查询里补 animation-iteration-count: 1 !important;(标准做法),或效仿 landing 在 classroom/FallbackBlob 各自的 reduced-motion 块里对 infinite 动画直接 animation: none。
- **src/pages/classroom/classroom.module.css:4** (scrollbar) 全站唯一深色页没有声明 color-scheme: dark,Windows/Chrome 下 .stream、.stage 的滚动条按浅色主题渲染,一条亮色滚动轨横在夜自习黑板正中。
  - 修法: 在 .room 与 .holder 上加 color-scheme: dark;(仅作用于讲解舱,不违反『非全局暗色模式』的设计纪律),让 UA 滚动条/表单控件按深色渲染。
- **src/pages/classroom/classroom.module.css:55** (responsive) 顶栏在窄屏下退化:.topCourse 声明 flex:0 0 auto 不可收缩,会溢出 .topTitle 滑到「下课」按钮底下;两个按钮无 white-space:nowrap 会折成两三行。
  - 修法: 给 .quitBtn/.endBtn 加 white-space: nowrap; flex: none;;.topCourse 改为允许收缩省略(min-width:0 + overflow:hidden + text-overflow:ellipsis)或在窄屏媒体查询里 display:none(参照 AppShell 520px 处隐藏 brandName 的既有做法)。
- **src/pages/classroom/classroom.module.css:177** (overflow) 聊天气泡缺少 overflow-wrap,老师粘贴长 URL 或不带空格的长代码串时会把整个对话流撑出横向滚动条。
  - 修法: 给 .bubbleT 和 .bubbleX 加 overflow-wrap: anywhere;(或 word-break: break-word),保持 pre-wrap 不变。
- **src/pages/classroom/index.tsx:239** (responsive) 手机宽度下讲台横排被固定 170px 的小白画布挤爆:名牌逐字折行,busy 时「小白正在琢磨…」被压成一列两三个字宽的竖条。
  - 修法: 880px 断点内缩小头像(size 由页面按断点传 120 左右,或给 .boardFrame 内 canvas 容器加 CSS 覆盖 width/height),并给 .stage 加 flex-wrap: wrap 或在窄屏隐藏 .stageThinking(桌面对话流里本就有『琢磨中』指示)。
- **src/pages/classroom/index.tsx:274** (interaction-state) 打字机每 26ms 强制 scrollTop 置底,小白长回复播放的数秒内用户无法向上滚动回看之前的消息,滚动被反复拽回底部。
  - 修法: onTick 里先判断是否贴近底部(el.scrollHeight - el.scrollTop - el.clientHeight < ~40px)再置底;用户主动上滚时停止追踪。
- **src/pages/growth/growth.module.css:152** (responsive) 盲区图谱 SVG 在窄屏下整体等比缩小,节点标签缩到约 5px、节点直径约 11px,完全不可读也几乎点不中,而同页 hero 有 820px 断点、AppShell 也有 640/520px 断点,说明移动端在设计范围内。
  - 修法: 给 .mapSvg 设 min-width(如 640px)并把 .mapWrap 在窄屏改为 overflow-x: auto 横向滚动容器;或加 @media (max-width: 640px) 断点换用两行/纵向布局的 viewBox。至少保证标签渲染后 ≥ 11px、节点点击区 ≥ 40px。
- **src/pages/home/home.module.css:13** (interaction-state) CTA「带小白上课去」同样被 .enter 的 fill-mode: both 钉死 transform,hover 只变色不上浮,交互反馈半残。
  - 修法: 同上一条:.enter 的 fill-mode 改为 backwards,或把 to 帧的 transform 从关键帧中移除(只保留 from 帧 + backwards)。
- **src/pages/landing/landing.module.css:340** (design-language) 三幕流程的幕次编号「第一幕/第二幕/第三幕」用朱砂色,但它既非误区也非警示,违反「朱砂只给误区/盲区/警示」的色彩纪律,稀释警示色。
  - 修法: 改为与其他 kicker 一致的 `var(--ink-faint)`,或用主动作色 `var(--azure)` 做层级强调。
- **src/pages/landing/landing.module.css:376** (interaction-state) 机制卡的 .card 的 transition 简写覆盖了 .reveal 的 transition,导致「教学机制」六张卡滚动显现时没有淡入上移动画,直接闪现,且 stagger 延迟失效。
  - 修法: 把 .card 的 transition 改为不与 reveal 冲突的写法,例如 `.card { transition: background var(--t-fast) var(--ease-out), opacity var(--t-slow) var(--ease-out), transform var(--t-slow) var(--ease-out); }`,或把 hover 底色的过渡放到 `.card:hover` 之外的独立子元素上。
- **src/pages/login/login.module.css:94** (inconsistency) 朱砂在三个页面被当作装饰点缀色(login 眉题、landing 机制印章/实录印、prep 书单小印),跨页累积稀释了「朱砂=警示」的全站语义。
  - 修法: 品牌印章(SVG 白文印)可保留朱砂血统,但文字眉题与类型徽章这类中性标识改用 `--azure`/`--ink-faint`;若坚持「朱文印」意象,至少收敛到每页一处。
- **src/pages/login/login.module.css:135** (interaction-state) 输入框为透明底+底线样式,但全站没有任何 :-webkit-autofill 覆写,浏览器自动填充会在宣纸底上糊出一块不透明的淡蓝/淡黄色块,登录页恰是最常被自动填充的页面。
  - 修法: 在 login.module.css 增加:.input:-webkit-autofill, .input:-webkit-autofill:hover, .input:-webkit-autofill:focus { -webkit-text-fill-color: var(--ink); box-shadow: inset 0 0 0 1000px var(--paper); caret-color: var(--ink); } 用纸色内阴影盖掉 UA 填充色。
- **src/pages/prep/prep.module.css:543** (design-language) 延伸书单的 kind 徽章「朱文小印」把朱砂用在非误区/警示内容上,且与同一卷宗内表示警示的朱砂标签(与错题相关/误区剧本)同屏并存,稀释警示语义。
  - 修法: 书单小印改为墨青白文印(border/color 用 var(--azure) 或 var(--ink-soft)),把朱砂留给误区语境;若坚持印章红,需在 DESIGN.md 中显式豁免'印章'场景并全站统一。
- **src/pages/prep/prep.module.css:613** (interaction-state) 自检清单复选框把原生 input 设为 opacity:0 后没有任何 :focus-visible 补偿,键盘聚焦完全不可见;且整页 819 行 CSS 没有一条 :focus-visible,与 home/landing/shell 的既有惯例不一致。
  - 修法: 加 .checkLabel input:focus-visible + .checkBox { outline: 2px solid var(--azure); outline-offset: 2px; },并给 .probeBtns button、.collapseHead、.step、.primaryBtn/.ghostBtn* 补齐与 home/shelf 一致的 :focus-visible 样式。
- **src/pages/prep/prep.module.css:688** (contrast) 备课五步侧栏的锁定步骤在 --ink-faint 文字上再叠 opacity:0.72,有效对比度跌到约 2.38:1,步骤名接近不可读。
  - 修法: 去掉整体 opacity,改用 color: var(--ink-faint) 配 .stepDot 变灰即可表达锁定;若要更弱,opacity 不低于 0.9 或单独调 stepDot。
- **src/pages/review/Radar.tsx:72** (inconsistency) 雷达维度分值按百分制显示(如「85」),而旁边的增量却按 0–1 原始小数显示(如「+0.12」),同一行两种量纲,用户看起来像单位错了。
  - 修法: 增量同样换算成百分制:{d > 0 ? '+' : ''}{Math.round(d * 100)},显示为「85 +12」。
- **src/pages/teacher/teacher.module.css:89** (responsive) 「讲不清 Top5」墨条图用 720 宽 viewBox 整体等比缩放,窄屏(手机)下 14px 的知识点楷体标签被压到约 6px,完全不可读;本页也是全站唯一没有小屏(<980px)媒体查询的页面。
  - 修法: 窄屏下不要让文字随 viewBox 缩放:方案一,把墨条图改为 HTML/CSS 实现(每行 label + 一个宽度百分比的 div 墨条),文字用 var(--fs-small)/var(--fs-tiny) 保持物理字号;方案二,保留 SVG 但在 <560px 媒体查询里换用更小的 CHART_W(通过 CSS 自定义属性或 matchMedia)并把标签移出 SVG。顺带可让 14px 归拢到与 --fs-small(13px)一致,消除与周边正文的 1px 漂移。
- **src/pages/teacher/teacher.module.css:155** (overflow) 泄漏率卡的「裸 prompt → 六层防线 → 白名单」对比行在默认桌面双栏布局下必然换行,横向箭头孤零零挤在竖向堆叠的两个大数字之间,答辩收尾页的核心对比构图在桌面端从不成立。
  - 修法: 把 .leakCard 的 padding 降为 var(--gap)(卡内 56px 边距对 400px 宽的卡过于铺张),.leakRow 的 gap 降为 var(--gap-tight);或改为竖排设计:两个数字上下排,箭头改为「↓ 六层防线」竖向连接符,用 @container/媒体查询在窄栏时切换,保证箭头方向始终与排布方向一致。

### LOW

- **src/components/shell/AppShell.module.css:39** (responsive) 宣传页透明头部内容贴视口两缘,而正文限宽 74rem 居中;宽屏(约 1280px 以上)时品牌落款与「进入书斋」药丸和海报栅格错位约 70px+,透明无界栏的设定放大了这种脱节感。
  - 修法: 给 .landing .header 内加一层与 .page 相同的 `max-width: 74rem; margin-inline: auto;` 包裹(或对 .landing .header 直接设同款 max-width + 对齐 padding),让透明头部落进海报栅格。
- **src/components/shell/AppShell.module.css:161** (dead-css) .link 里为「按钮形态」准备的 background/border/cursor 重置是死代码——代码库中该类只用于 NavLink 锚点,从未用在 button 上,注释已过时。
  - 修法: 删除这三条声明与过时注释;若未来真要按钮形态再加回。
- **src/components/shell/AppShell.module.css:216** (interaction-state) focus-visible 样式在外壳控件间不统一:齿轮/关闭/模式卡用 2px 实线 azure 外框,导航链接、登入药丸、用户按钮落回浏览器默认焦点环,testBtn 则只有 3px 的 10% 透明 azure-wash 阴影(几乎看不见)。
  - 修法: 统一为 outline: 2px solid var(--azure); outline-offset: 2px 的焦点约定,testBtn 移除 outline:none 改用同款实线外框。
- **src/components/shell/AppShell.module.css:271** (design-language) 「退出」按钮悬停用朱砂着色,朱砂按设计纪律只给误区/盲区/警示,退出登录不属于三者,稀释了朱砂的警示语义;且该规则在黑板模式下也生效,深底上朱砂小字对比不足。
  - 修法: 悬停色改用 var(--ink)(黑板下 var(--chalk)),或至少补 .board .userBtn:hover .userExit 覆盖。
- **src/components/shell/AppShell.module.css:287** (interaction-state) 齿轮按钮 1.9rem(约30px)与抽屉关闭按钮(约28×26px)触控目标均小于 40px 基线,移动端不易点中。
  - 修法: 把可点区域扩到 ≥2.5rem(视觉尺寸不变,用 padding 或伪元素扩大命中区)。
- **src/components/shell/SettingsDrawer.module.css:107** (interaction-state) 台词引擎三张模式卡没有 :hover 反馈,且声明的 transition: transform 是死代码(没有任何状态改变 transform),选中态的边框/底色变化则完全无过渡、生硬跳变。
  - 修法: 把 transition 改为 border-color/background/color(var(--t-fast) var(--ease-out)),并补 .modeBtn:hover { border-color: color-mix(in oklab, var(--ink) 25%, transparent) } 之类的轻反馈。
- **src/components/xiaobai/FallbackBlob.module.css:16** (inconsistency) WebGL 降级团子的黑板变体注释写『粉笔柔光』,实际用的是 --ink-wash(8% 深墨),深底上深色投影完全不可见,柔光形同虚设。
  - 修法: 换成粉笔系半透明光晕,如 box-shadow: 0 0 32px oklch(from var(--chalk) l c h / 0.15); 或在 tokens.css 增加 --chalk-wash 令牌后引用。
- **src/components/xiaobai/XiaobaiStage.module.css:3** (dead-css) XiaobaiStage 组件及其样式文件是死代码:全站没有任何页面引用,讲解舱实际用的是固定 170px 的 XiaobaiAvatar
  - 修法: 删除 XiaobaiStage.tsx + XiaobaiStage.module.css 及 re-export;或真正让讲解舱左讲台改用这个铺满式舞台(它反而比固定 170px 更能适配窄屏)
- **src/pages/classroom/classroom.module.css:41** (interaction-state) 按钮过渡声明与实际 hover 变化不匹配:多个按钮 transition 的属性从不变化,真正变化的 color/border-color 反而瞬跳,兄弟按钮之间手感不一致。
  - 修法: 统一为 transition: color var(--t-fast) var(--ease-out), border-color var(--t-fast) var(--ease-out), transform var(--t-fast) var(--ease-out);(按各自实际变化的属性列全),删掉从不生效的 opacity 项。
- **src/pages/classroom/classroom.module.css:198** (token-violation) 小白气泡字号硬编码 1.05rem,正好是 --fs-sub 的 clamp 下限,绕过字阶令牌造成微妙漂移。
  - 修法: 若意图跟随字阶,改为 font-size: var(--fs-sub);若确要固定,在 tokens.css 增补语义令牌(如 --fs-speech)并引用,不留裸值。
- **src/pages/classroom/classroom.module.css:312** (interaction-state) 输入框 :focus 移除 outline 后仅靠边框变色兜底,且整页按钮都没有 :focus-visible 样式,与 AppShell 已确立的 azure 焦点环规范不一致。
  - 修法: 给讲解舱交互控件补 :focus-visible { outline: 2px solid var(--chalk-amber); outline-offset: 2px; }(板书场景用 chalk-amber 替代 azure),textarea 的 :focus-visible 同样给可见焦点环。
- **src/pages/classroom/classroom.module.css:323** (interaction-state) :focus-visible 全站覆盖不齐:仅 AppShell/SettingsDrawer/home/shelf/landing 五个文件定义了品牌化焦点环,讲解舱/备课/复盘/成长/看板/登录六页的全部按钮回落到浏览器默认蓝色 UA 焦点环,与书斋视觉冲突。
  - 修法: 统一在各页交互控件补 :focus-visible { outline: 2px solid var(--azure)(黑板场景用 var(--chalk)); outline-offset: 2px; },或在 index.css 写一条全局 button/a 的 :focus-visible 兜底。
- **src/pages/classroom/classroom.module.css:358** (dead-css) .primaryBtn 与 .sendBtn、.ghostBtn 与 .quitBtn 是近乎逐行重复的样式块,只差 padding 一两个值,维护时容易改一漏一。
  - 修法: 用 CSS Modules 的 composes 抽出 .chalkBtn / .lineBtn 基类,各自只保留 padding 等差异声明,顺带统一 hover/transition 行为。
- **src/pages/classroom/classroom.module.css:406** (inconsistency) 同一份材料 Markdown 在三处渲染样式漂移:prep 材料包代码块有描边,classroom 查书抽屉与 review 补学课无描边,内距三套。
  - 修法: 三处代码块统一为一套(建议带 1px paper-edge 描边 + 0.7rem 0.9rem 内距),因为它们渲染的是同一批课程材料。
- **src/pages/growth/KnowledgeMap.tsx:86** (inconsistency) 节点同时挂了 SVG <title>(原生浏览器 tooltip)和自绘 hoverText,悬停约 1 秒后会出现两个内容几乎相同的提示叠在一起。
  - 修法: 去掉 <title>,改用 aria-label 挂在 <g> 上保留无障碍语义,只保留自绘 hoverText 一个可见提示。
- **src/pages/growth/growth.module.css:134** (dead-css) 两处相同选择器紧挨着重复声明:.personaCard:hover 写了两条(134/136 行),.btnGhost:hover:not(:disabled) 也写了两条(336/338 行),属于可合并的冗余块。
  - 修法: 各自合并为一条声明块:`.personaCard:hover { transform: translateY(-1px); border-color: var(--azure); }` 与 `.btnGhost:hover:not(:disabled) { transform: translateY(-1px); background: var(--azure-wash); }`。
- **src/pages/growth/growth.module.css:271** (inconsistency) 证据链时间线组件在 growth 与 review 是同一块复制代码,但行距漂移:growth 用 --gap-hair,review 用 --gap-tight,同一「时间线」两页密度不同。
  - 修法: 统一 gap(证据条目多为单行,--gap-hair 的紧凑档更合适),并在两处注释互相引用防再漂移。
- **src/pages/growth/growth.module.css:294** (inconsistency) 金句卡在 growth 与 review 是复制粘贴后漂移的两份:卡片 max-width 28rem vs 30rem,金句字号一个 fs-sub 一个跟随正文,同一「金句」两页两个样子。
  - 修法: 两处取齐(建议 max-width 30rem + font-size var(--fs-sub)),或按 DESIGN.md 页面自包含原则至少在注释里互相标注同步义务。
- **src/pages/growth/growth.module.css:325** (interaction-state) 成长页所有可交互元素(personaCard、btnGhost、cornerBtn、cornerYes、cornerNo、SVG 节点)都没有 :focus-visible 样式,而 home/landing/shell/SettingsDrawer 都定义了自定义焦点环,键盘导航时本页外观与其他页不一致(退化为浏览器默认轮廓)。
  - 修法: 为 .personaCard、.btnGhost、.cornerBtn、.cornerYes、.cornerNo 补上与其他页一致的 :focus-visible 样式(如 outline: 2px solid var(--azure); outline-offset: 2px);SVG 节点若要可聚焦需给 <g> 加 tabIndex 并配 focus 样式。
- **src/pages/growth/growth.module.css:356** (dead-css) .cornerBtn 声明了 transition: opacity,但 :hover 改变的是 color,过渡永远不触发,颜色直接跳变,transition 是死代码。
  - 修法: 删掉这条 transition;若想要柔和的 hover 反馈,改为 hover 时降低/恢复 opacity(如默认 opacity: 0.85,hover 时 1),与 transition: opacity 匹配且符合设计规范。
- **src/pages/growth/growth.module.css:363** (interaction-state) 重置二次确认的「确认重置/算了」按钮点击区极小(约 21px 高,fs-tiny 11px 字 + 0.05rem 竖向内边距),低于 40px 触控标准,且与同区 .cornerBtn、.btnGhost 不同——完全没有 :hover 反馈。
  - 修法: 给两个按钮增加竖向内边距(如 0.3rem 0.75rem)拉到至少 ~32-40px 高,并补 hover 态(cornerYes 用 var(--cinnabar-wash) 背景、cornerNo 用 var(--ink-wash)),与页内其他按钮的反馈语言一致。
- **src/pages/growth/index.tsx:185** (interaction-state) 证据链面板的收起动画永远不会被看到:再次点击节点取消选中时,`selNode` 立刻变 null 导致内容瞬间卸载,grid-template-rows 1fr→0fr 的过渡在空容器上进行,视觉上是突兀消失而非设计好的滑动收起。
  - 修法: 把面板内容与展开状态解耦:用一个 `lastSelected` 状态缓存最后选中的节点,收起时仍渲染缓存内容,仅切换 .open 类,让 0fr 过渡带着内容收起;过渡结束(onTransitionEnd)后再清空缓存。
- **src/pages/home/shelf.module.css:84** (token-violation) 木色搁板 .rail 硬编码 3 个新造 oklch 木色,并重复手写墨色阴影字面量,绕开 tokens.css,违反「一切颜色从 tokens 取」的硬性禁令。
  - 修法: 在 tokens.css 增加 --wood / --wood-deep / --wood-light 三个木色令牌(其他页若做书架/桌面质感可复用),阴影直接引用 --shadow-lift 或新建 --shadow-shelf 令牌,.rail 全部改引用。
- **src/pages/home/shelf.module.css:139** (duplication) @keyframes shelfRise 与 home.module.css 的 @keyframes rise 逐字重复(opacity 0→1 + translateY(8px)),同一入场动效在同页两处维护。
  - 修法: 把入场关键帧提升为全局(在 index.css 用普通 @keyframes 定义如 `rise-in`,CSS Modules 中以 `animation: rise-in ...` 引用需配 :global,或直接建一个 shared.module.css 供两文件 composes),reduced-motion 覆盖随之收拢为一处。
- **src/pages/home/shelf.module.css:201** (contrast) jade 作小字号文字色处于 AA 边缘之下:书脊「已出师」状态字 3.55:1(11px),备课页 .fbGood 4.09:1(13px 加粗)。
  - 修法: 把 --jade 的 L 从 0.56 降到 ≈0.50(对 paper 约 5:1)后全站统一受益;色块用途(band/halo)对文字令牌不敏感,可直接改主令牌。
- **src/pages/home/shelf.module.css:232** (dead-css) .fresh 的 background 与 .spine 默认背景完全相同,是一条无效的重复声明。
  - 修法: 删除 .fresh 规则(TSX 里 styles.fresh 拼接结果为 undefined 也无害,或保留空类名仅作语义标记并注释说明)。
- **src/pages/home/shelf.module.css:237** (contrast) locked 书脊双重压暗(ink-faint 文字 + 整体 opacity 0.55),11px 的「未开放」标签有效对比度约 2:1,基本不可读。
  - 修法: 压暗只取一种手段:要么 opacity 提到 0.7 左右并保留 ink-faint,要么去掉整体 opacity、单独把标题/状态字设为 ink-faint,保证「未开放」在 11px 下仍可辨认。
- **src/pages/landing/index.tsx:104** (empty-state) 泄漏率报告缺失的降级态视觉不成立:会渲染「待测 → 待测」,且左侧「待测」仍带 2px 朱砂删除线,像排版错误而非待测状态。
  - 修法: 降级时整格改为单一「待测」文案(不渲染 pair 结构),或给非数值态加一个去删除线、去黛绿的降级类。
- **src/pages/landing/landing.module.css:83** (design-language) 巨字水印呼吸动画用 ease-in-out 缓动与裸写 7.5s 时长,违反 DESIGN.md「只用 var(--ease-out)」的缓动禁令。
  - 修法: 呼吸类往复动画若必须对称缓动,至少改用 `var(--ease-out)` 配 alternate(视觉差异极小),或在 tokens.css 显式增补一个环境动画缓动令牌,而非内联字面量。
- **src/pages/landing/landing.module.css:194** (interaction-state) 次级 CTA「看看它怎么运作 ↓」(.ctaGhost)缺 :focus-visible 定制样式,与同排 .cta 及 .bandCta、.shelfStrip 的墨青描边焦点环不一致;且触达高度约 28px,小于 40px 触摸目标。
  - 修法: 补 `.ctaGhost:focus-visible { outline: 2px solid var(--azure); outline-offset: 3px; }`,并加 `padding-block` 或 `min-height` 把可点区域撑到 40px 以上(视觉可用负 margin 抵消)。
- **src/pages/landing/landing.module.css:231** (dead-css) 《学记》竖排引文、heroAvatar、foot 三块样式在 home 与 landing 逐字复制(含窄屏横排回退),是最应抽公共类的跨页复制块。
  - 修法: 《学记》引文是全站精神锚点且已有两份拷贝,建议抽成 `components/quote/StudyQuote` 自带样式,或至少放进一个共享的 quote.module.css 供两页 import。
- **src/pages/landing/landing.module.css:529** (inconsistency) 章节节奏不一致:数字带 .proof 无上边距/上边框,与上文实录只隔 1 倍 gap-section,而其余章节两两间距为 2 倍;书架节 .shelfSec 底部同样只留 1 倍间距接 .band,节奏忽紧忽松。
  - 修法: 若非刻意的「尾注紧排」,给 .proof 补 `padding-top: var(--gap-section)`(其 border 已由 .proofRow 的 border-block 承担),给 .shelfSec 补 `padding-bottom: var(--gap-section)` 并把 .band 的 margin-top 去掉,统一 2× 节奏;若是刻意压缩,至少两处方向应一致并加注释说明。
- **src/pages/landing/landing.module.css:687** (token-violation) .miniRail 木色搁板用三个裸写 oklch 色值,且与门厅书架 shelf.module.css 逐字重复,违反「一切颜色从 tokens.css 取」的硬性禁令并存在跨文件漂移风险。
  - 修法: 在 tokens.css 增设 `--wood`/`--wood-deep`/`--wood-sheen`(或 --shelf-rail 系列)令牌,两个文件统一引用。
- **src/pages/landing/landing.module.css:737** (inelegant-css) 两处黑板渐变与两处 CTA 内距字面量逐字重复,维护时容易只改其一。
  - 修法: 在文件顶部(或 tokens.css)提取 `--board-scene-bg` 与 `--cta-pad-y/--cta-pad-x` 自定义属性,或用一个共用类承载 CTA 公共样式,变体类只覆盖配色。
- **src/pages/login/index.tsx:37** (interaction-state) status==='unknown'(网关探测中)时登录页直接渲染完整表单,若随后判定为 standalone/authed,整个双栏卡片会闪换成 notice 提示卡——同一场景 RequireAuth 已做了防闪处理,登录页没有对齐。
  - 修法: 对 status==='unknown' 同样 return null(或渲染与 .split 同尺寸的占位骨架),与 RequireAuth 的防闪策略保持一致。
- **src/pages/login/login.module.css:9** (design-language) 登录页把 --cinnabar-wash 用作纯装饰性背景晕染、--cinnabar 用作栏目眉标,均非警示语义,与朱砂纪律相悖。
  - 修法: 装饰晕染第二枚渐变改 var(--amber-wash) 或 var(--ink-wash);.eyebrow 改 var(--azure) 或 var(--ink-faint)。若坚持印章叙事,红色应只留给页内已有的 .plateSeal 印章本体。
- **src/pages/login/login.module.css:28** (dead-css) 同名 @keyframes rise 在 8 个 CSS module 各存一份,其中 login 的位移量(12px)与其余 7 份(8px)悄然不一致。
  - 修法: 在 index.css 定义一份全局 `@keyframes rise`(CSS Modules 中各模块用 `animation: rise ...` 前加 :global 或直接放全局层),位移统一 8px,删除 8 份拷贝。
- **src/pages/login/login.module.css:154** (layout) errorSlot 预留高度(1.25rem)小于错误文案实际行高(≈1.42rem),报错出现时登入按钮仍会下跳约 3px;且 .error 复用 rise 动画(translateY 12px),入场瞬间红字会短暂压到按钮上沿——防跳动的占位槽没有真正防住。
  - 修法: min-height 改为 calc(var(--fs-small) * 1.75)(或直接 1.5rem);并给 .error 单独一个小位移的入场关键帧(如 translateY(4px))或只做 opacity 渐显,避免与下方按钮重叠。
- **src/pages/login/login.module.css:165** (interaction-state) .submit/.backLink/.noticeLink 均无 :focus-visible 样式,键盘聚焦时落回 UA 默认焦点环,与全站既有的 azure 描边焦点约定不一致——而登录页恰是键盘操作最密集的页面。
  - 修法: 补 .submit:focus-visible, .backLink:focus-visible, .noticeLink:focus-visible { outline: 2px solid var(--azure); outline-offset: 2px; },与齿轮按钮的焦点语言对齐。
- **src/pages/login/login.module.css:191** (interaction-state) 提交中(busy)与「不可提交」共用 :disabled 的 45% 透明外观,慢网络登录时主按钮看起来像被禁用/坏掉,而不是正在加载。
  - 修法: 给按钮加 data-busy 属性,增加 .submit[data-busy='true'] { opacity: 1; cursor: progress; } 并可让「正在验帖…」的省略号做逐点动画,使加载态与禁用态视觉可分。
- **src/pages/login/login.module.css:208** (interaction-state) 「← 先随便看看」链接继承 .foot 的 --fs-tiny(11px)且无任何内边距,触控高度约 19px,远低于 40px 最小点按目标,而它是移动端未登录用户唯一的逃生入口。
  - 修法: 给 .backLink 加 padding-block: 0.55rem 并用负 margin 抵消视觉占位(margin-block: -0.55rem),点按区达标且不破坏排版节奏。
- **src/pages/login/login.module.css:294** (dead-css) 窄屏媒体查询里的 writing-mode: horizontal-tb 是死代码——基础 .plateMotto 从未设置竖排 writing-mode,所谓「竖排铭」实际是用 <br> 逐字断行模拟的。
  - 修法: 二选一:删掉这行死声明;或把桌面端改为真正的 writing-mode: vertical-rl 并去掉 <br>(窄屏再切回 horizontal-tb,此时该声明才有意义),同时兑现「竖排铭」的设计意图。
- **src/pages/prep/prep.module.css:5** (inconsistency) 六个页面的版心 max-width 各不相同(1120px/1152px/1160px/1184px)且 rem 与 px 混用,页面间跳转时内容列宽度可见地跳动。
  - 修法: 在 tokens.css 增加 `--page-max: 72rem` 与 `--page-pad: clamp(1.25rem, 4vw, 3rem)`,除 landing 有意做更宽海报外,study/prep/review/growth/teacher 统一引用,消除导航时的版心跳动。
- **src/pages/prep/prep.module.css:10** (inconsistency) 「内容不存在」空态三个页面三种规格:review 是标题+说明+主按钮的完整空态,prep 只有一行裸文本,classroom 居中一行字,同一语义体验落差明显。
  - 修法: 把 review 的 empty 模式(标题 + 一句叙事 + 主按钮 CTA)复制到 prep 的 notFound 与 classroom 的 holder,文案保持各页气质即可。
- **src/pages/prep/prep.module.css:51** (magic-number) 同一文件里用两个不同的魔法数近似同一个吸顶导航高度:锚点让位 4.25rem,侧栏让位 3.4rem+0.75rem=4.15rem,头部高度本身没有 token。
  - 修法: tokens.css 定义 --header-h(或在 AppShell 上写 CSS 变量),三处(prep L51/L646、review L81)统一 calc(var(--header-h) + var(--gap-tight))。
- **src/pages/prep/prep.module.css:129** (interaction-state) 核心交互按钮触控目标不足 40px:摸底快测的对/错按钮约 34px 高,侧栏五步锚点钮约 26px,重新测一遍(ghostBtnSm)约 28px,移动端易误触。
  - 修法: 对/错按钮 padding 提到 0.55rem 0(≈40px);.step 与 .ghostBtnSm 至少加 min-height: 2.5rem 或增大纵向 padding,尤其在 ≤560px 断点。
- **src/pages/prep/prep.module.css:173** (inconsistency) 同一语义的墨青主按钮在五处有五套实现:内距、hover 行为(变深/上浮/下压)与命名(primaryBtn/btnPrimary/cta/submit)互相矛盾。
  - 修法: 统一主按钮规范:一组 padding、hover = 变 `--azure-deep` + translateY(-1px)、active 下压,各页类名对齐(建议统一叫 primaryBtn),或抽成 index.css 里的全局 .btn-primary。
- **src/pages/prep/prep.module.css:184** (interaction-state) 主按钮禁用态是纸白文字压 --dust 底,对比度仅 2.29:1,「开始讲课」等按钮在未达条件时几乎看不清字。
  - 修法: 禁用态改成低饱和但可读的组合,如 background: var(--paper-edge); color: var(--ink-faint);(约 3:1 以上),或保持 azure 底加 opacity: 0.55(与 review .btnPrimary:disabled 的做法统一)。
- **src/pages/prep/prep.module.css:192** (interaction-state) 次级幽灵按钮跨页外观相反:prep 灰边灰字、growth 青边青字;且 prep 的 transition 只写了 transform,hover 实际变的 color/border-color 无过渡直接跳变。
  - 修法: 统一次级按钮为一种形态(建议 growth 的青描边版);prep `.ghostBtn` 的 transition 改为 `color var(--t-fast), border-color var(--t-fast)`。
- **src/pages/prep/prep.module.css:456** (token-violation) 小方签/徽章的圆角跨页分裂:growth/review 用 var(--radius-s)(4px),prep/classroom 一律硬编码 2px,且内距有 0 4px/0 5px/0 6px/0 0.45rem 四种。
  - 修法: 若小签确需比 radius-s 更锐利,在 tokens.css 增 `--radius-tag: 2px` 全站引用;否则统一改 var(--radius-s)。内距收敛到一种(如 0 0.4rem)。
- **src/pages/prep/prep.module.css:496** (dead-css) .code 与 .md pre 是两段近乎逐字重复的代码块样式(仅 margin 不同),后续调整极易只改其一造成讲义内代码与例题代码观感分叉。
  - 修法: 合并:让例题 <pre> 复用同一个类(如 .codeBlock),或写成 .md pre, .code { 共同声明 } 再各留一行 margin 差异。
- **src/pages/prep/prep.module.css:586** (token-violation) 延伸书单导读 .refNote 硬编码宋体字体栈,在令牌体系(--font-display/--font-body/--font-code)之外私自引入第三种字体,跨平台观感不可控。
  - 修法: 改用 var(--font-body) 或在 tokens.css 增设 --font-serif 令牌后引用;若坚持宋体层次,至少让字体栈进入令牌统一管理。
- **src/pages/prep/prep.module.css:646** (inconsistency) 吸顶偏移用了两个互不相同的魔法数(4.25rem 与 3.4rem)去近似同一个 shell 导航栏高度,且与 review 页用令牌表达偏移的做法不一致,导航栏一改高就要在两处人肉找齐。
  - 修法: 在 tokens.css 或 AppShell 中定义 --header-h(或由 shell 写入 CSS 变量),两处改为 scroll-margin-top: calc(var(--header-h) + var(--gap)) / top: calc(var(--header-h) + var(--gap-tight))。
- **src/pages/prep/prep.module.css:705** (token-violation) scrollspy 当前项圆点外圈用裸 oklch() 字面量复刻墨青色相,绕过令牌。
  - 修法: 改为 box-shadow: 0 0 0 3px color-mix(in oklab, var(--azure) 25%, transparent),与 var(--azure) 保持联动。
- **src/pages/prep/prep.module.css:763** (inconsistency) 全站断点值漂移严重:同一「内容列+眉批列」双栏收拢断点在各页分别是 820/880/896/920/960/980px 六个值,且 px 与 rem 单位混用
  - 修法: 在 tokens.css 注释里约定 3 档断点(建议 60rem/45rem/35rem,与 landing 对齐)并统一各页媒体查询取值与单位;CSS Modules 不支持变量断点,靠约定+代码评审维持
- **src/pages/review/index.tsx:165** (inconsistency) 同一页两种展开箭头机制:补学微路径用文本换字「▾/▴」,证据链用 CSS 旋转的「▸」(.chev),视觉语言不统一。
  - 修法: 补学微路径的箭头也改用 .chev 旋转符号(▸ + rotate(90deg)),文案里不再嵌 ▾/▴。
- **src/pages/review/review.module.css:81** (z-index) 眉批侧栏 sticky 偏移小于全局吸顶导航高度,在 961–1300px 宽度下「批阅讫」印章滚动时会滑到半透明毛玻璃导航条底下被遮住。
  - 修法: 把吸附偏移改为跟随导航高度,例如在 tokens.css 定义 --header-h(约 3.4rem),侧栏用 top: calc(var(--header-h) + var(--gap));或直接 top: max(var(--gap-loose), 4rem)。
- **src/pages/review/review.module.css:117** (responsive) 复盘页 ≤960px 直接 display:none 丢掉整条眉批栏(「批阅讫」印章与 4 条批注文案),而姊妹页备课页对同类眉批栏是降级堆叠,同一招牌构图两种处理
  - 修法: 与备课页统一:窄屏把 .rail 改为静态块堆在正文之后(印章可藏,批注文案保留),而不是整栏删除
- **src/pages/review/review.module.css:235** (responsive) 雷达 SVG 文字用画布用户单位定字号,窄屏整图等比缩小后维度标签实际只有约 8px,小屏上基本不可读。
  - 修法: 加窄屏媒体查询放大 SVG 内文字(如 max-width: 640px 时 .radarLabel { font-size: 20px } 并把标注半径 1.26 略收),或缩小 viewBox 四周留白提高图形占比。
- **src/pages/review/review.module.css:313** (interaction-state) 整个复盘页没有任何 :focus-visible 样式,交互元素键盘聚焦时退回浏览器默认框,与外壳组件的墨青自定义焦点环不一致。
  - 修法: 为上述四类可聚焦元素补一条共享规则:outline: 2px solid var(--azure); outline-offset: 2px,与外壳保持同一焦点语言。
- **src/pages/review/review.module.css:316** (interaction-state) 补学与证据链两个纯文字展开按钮 padding: 0,可点区域高度仅约 26–33px,低于 40px 触控目标下限。
  - 修法: 给两个 toggle 加竖向内边距并用负 margin 抵消布局位移(padding: 0.5rem 0; margin: -0.5rem 0;),把命中区撑到 ≥40px 而不改变视觉节奏。
- **src/pages/review/review.module.css:382** (dead-css) .code 与 .lesson pre 两个规则块声明完全重复(背景、圆角、内边距、滚动、字号、行高六项全同),维护时改一处漏一处。
  - 修法: 合并选择器:.code, .lesson pre { … } 共享一份声明,margin 差异单独保留在 .code。
- **src/pages/review/review.module.css:415** (interaction-state) :focus-visible 覆盖跨页不一致:home/landing/AppShell 为按钮精心定义了焦点外框,prep/review/growth/classroom 的全部按钮均未定义,键盘遍历时体验割裂。
  - 修法: 在 index.css 加一条全局兜底 `:focus-visible { outline: 2px solid var(--azure); outline-offset: 2px; }`(讲解舱内用 --chalk 覆写),替代各页零散手写。
- **src/pages/review/review.module.css:494** (interaction-state) 证据链展开按钮声明了 opacity 过渡但 hover 实际改变的是 color,过渡永远不生效,悬停生硬跳变,与同页 .remedyToggle 的淡出反馈不一致。
  - 修法: 把 .evidenceToggle 的 transition 改为 color var(--t-fast) var(--ease-out)(或统一两个 toggle 的 hover 机制),消除死过渡声明。
- **src/pages/teacher/teacher.module.css:83** (dead-css) .muted 规则在 teacher/index.tsx 中从未被引用,是死代码。
  - 修法: 删除该规则;若本意是给次级说明文字复用,应改用已有的 .demoNote/.chartFoot/.tableFoot 语义类。
- **src/pages/teacher/teacher.module.css:121** (token-violation) 学情表 th/td 的内边距用了 0.35rem 与 0.4rem 两个互不一致的魔法数,且都紧贴令牌 --gap-hair(0.375rem)却各自漂移。
  - 修法: th/td 统一为 padding: var(--gap-hair) 0.6rem var(--gap-hair) 0(或都取 0.4rem 并在两处保持一致),消除同表内 1px 级别的节奏漂移;.seal 同步收敛到 --gap-hair。


## 驳回的 3 条(验证不成立)

- shelf.module.css:174 竖排书名溢出 — 当前数据最长 8 字,未越界。
- prep.module.css:644 sticky 眉批无 max-height — 实际视口下未复现溢出。
- growth.module.css:69 成长阶梯 375px 挤压 — 引文属实但视觉可接受。

## 审查副产物

- 本地 dev localStorage 新增了 2 个测试会话(T20260706-cd32 已下课 + 一个 0 轮已退出),演示前可用成长页「演示重置」清理。
- 完整 JSON(含 evidence/verdict 全文): scratchpad confirmed.json(会话级,已摘录至本文件)。
